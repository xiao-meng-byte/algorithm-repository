# 最小生成树
## 最小生成树是什么
**最小生成树**:在无向带权边中选择一些边，在保证**联通性** 的情况下，边的总权值最小

最小生成树可能不止一棵树，只要保证边的总权值最小，就都是正确的最小的生成树

有一条结论是:如果无向带权图有**n**个点，那么最小生成树一定有**n-1**条边

### 补充: 啥是无向带权图
```hash
补充：什么是无向带权图？（最详细大白话版）

【第一步：什么是“图”】
平时我们说的图，是照片或画片。但在计算机算法里，“图（Graph）”是一种数据结构。
它是由“点（顶点/节点）”和“线（边）”组成的。
想象一下：你在看一张全国地图。
地图上，每一个城市就是一个“点”；连接城市的高速公路就是一条“线”。
点和线连在一起，就构成了一个“图”。

【第二步：什么是“无向”】
“无向”的意思是：线是没有箭头的，是双向通道。
比如，城市 A 和城市 B 之间有一条路。
你可以从 A 开车去 B，也可以从 B 开车回到 A。这条路不分方向，可以随便走，这就是“无向”。
如果是一条单行道，只能从 A 走到 B，不能从 B 走回 A，那条线上就必须画一个箭头，那就变成了“有向”。

【第三步：什么是“带权”】
“带权”是这三个词里最关键的。它意味着，连接两个点的这条线上，附带有“数字”，这个数字就是“权值”。
在现实世界里，权值通常代表：距离（公里）、花费（钱）、时间（小时）等。
比如：
A 到 B 的高速公路长 100 公里，那这条线上的“权值”就是 100。
B 到 C 的山路很难走，要花 3 个小时，那“权值”就是 3。

【综合起来：无向带权图长什么样？】
如果我们用画图的方式，它长这样：
点 A ——（权重10）—— 点 B
点 B ——（权重5） —— 点 C
点 A ——（权重20）—— 点 C

注意看：
上面这三条线都没有箭头，说明它们是无向的。
上面三条线上的数字 10、5、20，说明它们是带权的。

【为什么必须要搞清楚“权值”？】
因为带权图通常用来解决“最优解”问题。
比如你要从 A 走到 C，你有两条路：
第一条：A 直接到 C，花费 20。
第二条：A 先到 B（花费10），再从 B 到 C（花费5），总共花费 15。
因为带权图有数字，我们就能一眼看出走第二条路更省钱。

【这跟最小生成树有什么关系？】
最小生成树的“生成树”，就是在一个图中挑出一些线，把这些所有的点都连起来，不能有漏掉的点（也就是不能有孤立点），而且连起来的整体不能有环（也就是不能绕圈）。
因为它是“生成树”，所以要求的点是全覆盖的。
“最小”的意思，就是挑出来的这些线，它们的权值加起来（总花费）必须是最小的。
所以最小生成树要做的，就是在一堆带权值的路里，选出一条“总花费最少，又能把所有城市连起来”的路线。

【一句话总结】
无向带权图 = 一堆点 + 一堆能双向走的线 + 线上标着数字（代价）。
它的存在，就是为了让我们能算账，找出最省钱的连接方案。
```
## kruskal 算法
* 好处就是连图都不用建立
* 做法:
  * 把所有的边，根据权值从大到小排序，从权值小的边开始考虑
  * 如果连接当前的边不会形成环，就选择当前的边
  * 如果连接当前的边会形成环，就不要当前的边
  * 考察完所有边之后，最小生成树就形成了
* 证明:
```hash
【第一步：认清我们要证明什么】
我们要证明：只要把所有边按权值从小到大排序，依次考察，能连就选，形成环就不选，最终选出来的边，一定能让所有点连通，而且所有边的权值之和一定是最小的。

【第二步：为什么“没有形成环”是我们唯一的底线？】
在图论里，要把 n 个点连起来，并且不绕圈（也就是没有环），恰好需要且只能需要 n-1 条边。如果你选多了，形成环了，就会浪费钱（因为环上多出来的那一条边，
其实是不需要的，你可以把它去掉，点依然是连通的）。
所以，算法每一步的底线都是：只要不构成环，我们就放心大胆地选。

【第三步：核心证明方法——替换法（最通俗的“斗地主”法）】
假设：
“算法选出来的边”叫 A 方案。
“真正的、绝对完美的、花钱最少的边”叫 B 方案。

如果 A 不是最完美的，那说明 A 的花费 > B 的花费。

【第四步：找第一根不同的边】
我们把所有的边按照权值从小到大排好队，把 A 方案和 B 方案摆在面前做对比。
从头开始找，找到第一根在 A 方案里和 B 方案里不一样的边。
为了好懂，我们把边上标上价格：A 选的这根边叫“便宜边”（因为我们是按从小到大挑的），B 选的这根边叫“贵边”。

【第五步：为什么 B 方案不可能更便宜？】
仔细想，如果我们现在把 B 方案里那根“贵边”剪掉，B 方案是不是就断了？答案是肯定的，因为它是连通图。
那我们要怎么把它重新连起来呢？
这时候，“便宜边”的作用就出来了。
因为 B 方案里的点和 A 方案里的点是相同的（都是相同的点集合），所以“便宜边”连接的那两个点，在 B 方案里一定可以通过其他的边连上。
我们把 B 方案里的“贵边”扔掉，换上 A 方案里的“便宜边”。
换完之后，B 方案依然是连通的！而且，因为“便宜边”的价格 ≤ “贵边”的价格，所以换完之后，B 方案的总花费不仅没变多，反而变少了（或者一样）。

【第六步：得出矛盾】
这就有意思了。
我们一开始假设 B 方案是“绝对完美的、花钱最少的”。
但是，我们通过“换上便宜边”，让 B 方案变得更便宜了！
这说明，B 方案根本不是最便宜的！这和我们一开始的假设矛盾了！

【第七步：终极结论】
因为只要 A 方案（算法方案）和 B 方案（完美方案）有差异，我们都能把 B 方案变得更便宜。
唯一的可能就是：A 方案和 B 方案根本没有差异。
所以，A 方案本身就是花钱最少的方案。

【总结：为什么“从小到大”是核心？】
正是因为我们是“从小到大”选的，所以当我们遇到分歧时，一定是：
算法手里握着“便宜边”，而完美的方案手里握着“贵边”。
算法手里的东西，永远比完美方案手里的东西好（或者不差），所以算法最终拼出来的总价，一定不会输给完美方案！
```
考虑一下图:
```bash
图如下:          
   (1)---1---(2)
    | \     / |
    |  \   /  |
    6   2 1   3
    |    \/   |
    |    ⑤    |
    |    /\   |
    |   /  \  |
    |  7    3 |
    |  /     \|
   (3)----4--(4)

对于这个图，我们可以用并查集的思想来看：

1. 首先我们有5个单独的集合 {1},{2},{3},{4},{5}。
根据权值，在我们排完序后，以 a, b, c, d, e, f... 来代表排完权值序之后的边（如果权值一样就顺便排）。
比如这个图我们就可以这样标：
(1) --a(权值为1)-- (2)
(2) --b(权值为1)-- ⑤
(1) --c(权值为2)-- ⑤
依次类推.............

2. 那么我们怎么判断有没有形成环呢？
我们可以用并查集判断是否在同一个集合里，来判断是否形成环。
就是我们在连线的时候，比如之前标的 a 连接了 (1),(2)，而 b 连接了 (2),(5)，
就已经可以把 {1, 2, 5} 视为一个集合了。

3. 接下来我们看连线 c，它连接了 (1),(5)，
那我们要不要连接上呢？
在之前的判断中，{1}、{2}、{5} 已经在同一个集合中了。
如果连接必成环，所以舍弃。

4. 接下来的连线规则也是一样：
每次看当前权值最小的边，如果这条边的两端连接的是不同的集合（find根节点不同），
就选择这条边（union合并集合）；
如果两端已经在同一个集合中（find根节点相同），
就说明连接后必然产生环，直接舍弃这条边。

5. 直到最后，我们把所有元素都视为同一个集合（并查集里的 sets == 1）时，
最小生成树就形成了！
```
### 例题 引用: 洛谷P3366
题目链接:https://www.luogu.com.cn/problem/P3366

这个题目其实已经就是上面所推导的思路了，所以代码如下:
```cpp
#include <bits/stdc++.h>
using namespace std;
const int MAXN = 10000;
vector<int> father(MAXN); // 并查集数组，father[i] 表示 i 的父亲节点
struct Edge {
    int u, v, w; // 定义一条边：起点 u，终点 v，权值 w
};
int n, m;
vector<Edge> edges; // 用 vector 存放所有输入的边
static void init(int n) { // 并查集初始化
    for (int i = 1; i <= n; i++) { // 遍历 1 到 n 号节点
        father[i] = i; // 一开始每个节点的父亲都是自己，形成独立的集合
    }
}
static int find(int x) { // 查找 x 所属集合的根节点（带路径压缩）
    if (x != father[x]) { // 如果 x 的父亲不是它自己，说明还没找到根
        father[x] = find(father[x]); // 递归寻找根，并把沿途节点直接连到根上，实现扁平化
    }
    return father[x]; // 返回根节点
}
static bool Union(int x, int y) { // 合并 x 和 y 所在的集合，判断是否形成环
    int px = find(x); // 找到 x 的根
    int py = find(y); // 找到 y 的根
    if (px != py) { // 如果根不相同，说明不在同一个集合
        father[px] = py; // 将 x 所在集合挂到 y 所在集合下面，完成合并
        return true; // 合并成功，这条边不会形成环，返回 true
    } else {
        return false; // 根相同，说明已经在同一个集合，连上会形成环，返回 false
    }
}
int main() {
    ios::sync_with_stdio(false); // 关闭 C 和 C++ 的输入输出同步，加速读取
    cin.tie(nullptr); // 解除 cin 和 cout 的绑定，加速输出
    cin >> n >> m; // 读入节点数 n 和边数 m
    init(n); // 初始化并查集
    for (int i = 0; i < m; i++) { // 遍历 m 条边进行输入
        int u, v, w; // 临时存储当前边的起点、终点、权值
        cin >> u >> v >> w; // 读入当前边的三个数据
        edges.push_back({u, v, w}); // 将当前边封装成结构体直接塞进 vector
    }
    sort(edges.begin(), edges.end(), [](const Edge& a, const Edge& b) { // 对边进行从小到大排序
        return a.w < b.w; // 按照权值 w 升序排序，符合 Kruskal 贪心原则
    });
    int ans = 0; // 记录最小生成树的总权值
    int cnt = 0; // 记录已经选入最小生成树的边的数量
    for (const auto& edge : edges) { // 基于范围的 for 循环，遍历排好序的所有边
        if (Union(edge.u, edge.v)) { // 判断当前边连接的两个节点是否在同一个集合（是否成环）
            cnt++; // 如果不成环，说明选择了这条边，选入的边数加 1
            ans += edge.w; // 累加这条边的权值到答案中
        }
    }
    if (cnt == n - 1) { // 图论性质：n 个节点的最小生成树一定包含 n-1 条边
        cout << ans << endl; // 如果达到了 n-1 条边，说明生成树构建成功，输出总权值
    } else {
        cout << "orz" << endl; // 如果达不到，说明图不连通，输出 orz
    }
    return 0; // 程序正常结束
}
```
### Kruskal 算法复杂度分析（基于代码）

#### 1. 时间复杂度

总时间复杂度由三个主要步骤决定：**排序、并查集初始化、遍历边并合并**。

| 步骤 | 代码对应 | 时间复杂度 | 说明 |
| :--- | :--- | :--- | :--- |
| **边排序** | `sort(edges.begin(), edges.end(), ...)` | **O(M log M)** | 对 M 条边按权值从小到大排序，这是整个算法最耗时的一步。 |
| **并查集初始化** | `init(n)` | **O(N)** | 遍历 N 个节点，将每个节点的父节点设为自己，线性复杂度。 |
| **遍历边并合并** | `for (const auto& edge : edges)` 调用 `Union` | **O(M * α(N))** | M 条边各执行一次并查集合并操作。由于路径压缩（扁平化）的存在，单次合并的均摊时间复杂度是 α(N)（反阿克曼函数，可视为常数级），因此非常快。 |

#### 总结（大 O 表示法）：

| 场景 | 时间复杂度 |
| :--- | :--- |
| **总时间复杂度** | **O(M log M)** |
| **最坏情况** | O(M log M) |
| **实际运行效率** | 排序是瓶颈。当 N 很大但 M 较小时，算法非常快；当 M 接近 N^2 的稠密图时，会比 Prim 算法慢。 |

---

#### 2. 空间复杂度

空间复杂度主要由存储图的边、并查集父节点数组以及少量辅助变量构成。

| 数据结构 | 代码对应 | 空间复杂度 | 说明 |
| :--- | :--- | :--- | :--- |
| **边集合** | `vector<Edge> edges` | **O(M)** | 需要存储所有输入的 M 条边，包含起点、终点和权值。 |
| **并查集数组** | `vector<int> father` | **O(N)** | 大小为 MAXN (N+1)，用于存储每个节点的父节点指针。 |
| **辅助变量** | `ans`, `cnt`, 临时变量 | **O(1)** | 常量级空间。 |

#### 总结：

| 场景 | 空间复杂度 |
| :--- | :--- |
| **总空间复杂度** | **O(N + M)** |

## Prim 算法
* 解锁的点的集合叫**set**(普通集合)，解锁的边的集合叫**heap**(小根堆)，**set**和**heap**初始都为空
* 可以从任意点开始，开始的点加入到**set**,开始点的所有边加入到**heap**
* 从**heap**中弹出权值最小的边e，查看边e所去往的点x
  * 如果x已经在**set**中了，那边e舍弃，重复步骤3
  * 如果x不在**set**中，边e属于最小生成树，把x加入**set**,重复步骤3
* 当**heap**为空了，最小生成树也就生成了
* 证明:
```hash
要证明的核心结论是：
每次从小根堆(heap)里弹出的、连接“集合内”和“集合外”的权值最小的边，一定属于某棵最小生成树(MST)。

第一步：把话说通俗（生活化比喻）
想象你是一个城市规划师，要把好几个村子(节点)用电缆(边)连起来，且电缆总造价(总权值)最低。
set：就是已经通了电的村子。
heap：就是所有从“通电村子”伸出来，准备连接到“没通电村子”的电缆方案。

Prim 算法的贪心策略就是：每次都只挑最便宜的那根电缆，把新村子拉进电网。

第二步：核心证明（反证法）
假设：
当前集合 set 里有一些点，外面还有没连的点。
我们从小根堆里弹出了一条权值最小的边，记作 e，连接 set 内的点 A 和 set 外的点 B，它的权值记作 W(e)。

假设我们的前提不成立：
假设“权值最小的边 e 不属于最小生成树”。

这意味着一棵真正的最小生成树(我们叫它 MST)里，没有边 e。但是因为 MST 要把所有点连起来，点 B 既然在 MST 里，它必须有一条边连到外面的某个点。假设 MST 里连接点 B 的那条边是 f，权值记作 W(f)。

关键反转来了：
因为我们的 e 是从 heap 里弹出的权值最小的边(连接 set 内外的边)，所以必然有：
W(e) <= W(f)

第三步：构造新树（替换法）
现在，我们在 MST 里把边 f 删掉，把边 e 加进去。
把 f 删掉后，MST 断开了，分成了两块。
加上 e 后，因为 e 连接了这两块，所以整棵树又连起来了，而且没有形成环(因为 B 是第一次进入 set)。

结论：
我们构造了一棵新的生成树，它的总权值 = MST 总权值 - W(f) + W(e)。
因为 W(e) <= W(f)，所以新树的权值 小于等于 MST 的权值。

既然新树也能连通，且权值更小(或相等)，那说明原来的 MST 并不是最“小”的！
这与“MST 是最小生成树”的前提矛盾！

所以，唯一的可能就是我们一开始的假设是错的：边 e 必须属于最小生成树！

第四步：图解证明（画个图帮你巩固）
假设我们用 set 已经“通电”了节点 1。
从节点 1 出发，发现连着节点 2 和 3：
边(1, 2)权值 = 5
边(1, 3)权值 = 10

按照 Prim，我们选边(1, 2)(因为 5 < 10)。

假设有一个“邪恶的最小生成树”偏偏没选边(1, 2)，而是选了边(1, 3)来连接节点2和3？
(尽管(1, 3)更贵)。为了连上节点2，它必须还要有一条边连到某个地方(比如边(2, 3)，权值20)。

此时这棵“邪恶树”包含：(1, 3)[权10]和(2, 3)[权20]，总权值30。
我们把“邪恶树”里的(2, 3)删掉，换上我们便宜的(1, 2)[权5]。
现在总权值是 10 + 5 = 15，比 30 小太多了！

所以，“邪恶树”不可能是最小的。最便宜的边必须被选上！

总结：一句话听懂
因为如果不选最便宜的那条边(e)，为了连通，总得选一条更贵的边(f)来代替它。把 f 换成 e 只会让总花费更少，所以 e 一定在最优解里。

补充：这也是为什么 Prim 算法是“贪心算法”
贪心算法的核心就是：每一步都做出当前看起来最好的选择。
Prim 每一步都在选当前最小的跨越边，由于上面证明的“切断/替换”原理存在，这个“看似局部最优”的选择，最终组合起来就是“全局最优”。
```
### 代码
对于洛谷P3366,也可以用**prim**算法来求:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int n, m;
    // 应对多组测试数据
    while (cin >> n) {
        // 【1. 建图】邻接表：graph[u] 存的是 {v, w}
        vector<vector<pair<int, int>>> graph(n + 1);
        cin >> m;
        for (int i = 0; i < m; i++) {
            int u, v, w;
            cin >> u >> v >> w;
            // 【无向图】必须双向加边
            graph[u].push_back({v, w});
            graph[v].push_back({u, w});
        }
        // 【2. Prim 核心数据结构】
        // 小根堆：存放所有“已选中点伸出去”的边，按权值从小到大排序
        // pair<int, int> 默认按 first 排序，所以把权值放第一位：{w, v}
        priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> heap;
        // 【3. 初始化】
        // 随便选一个点开始（这里选 1），把它的所有边扔进小根堆
        for (auto& edge : graph[1]) {
            heap.push({edge.second, edge.first}); // {cost, next}
        }
        vector<bool> set(n + 1, false); // set 记录节点是否已经加入生成树
        int cnt = 1; // 已经选中的节点数（因为从1开始，所以初始化为1）
        set[1] = true; // 标记起点 1 已加入
        int ans = 0; // 记录最小生成树的总权值
        // 【4. 贪心选边】
        while (!heap.empty()) {
            auto edge = heap.top(); // 每次弹出权值最小的边（贪心策略）
            heap.pop();
            int cost = edge.first;  // 这条边的权重
            int next = edge.second; // 这条边指向的目标点
            // 如果这个目标点还没被选过（不在 set 中）
            if (!set[next]) {
                cnt++; // 选中节点数 +1
                set[next] = true; // 标记已加入
                ans += cost; // 累加权值
                // 【核心步骤】：新点加入了，把它所有的邻边也扔进堆里
                for (auto& e : graph[next]) {
                    heap.push({e.second, e.first}); // {cost, next}
                }
            }
            // 如果目标点已经在 set 中，说明该边会成环，直接舍弃
        }
        // 【5. 判断连通性并输出】
        if (cnt == n) {
            cout << ans << endl;
        } else {
            cout << "orz" << endl;
        }
    }
    return 0;
}
```
### 1. 时间复杂度

| 步骤 | 代码操作 | 时间复杂度 | 说明 |
| :--- | :--- | :--- | :--- |
| **建图** | `graph[u].push_back({v, w})` | O(M) | 遍历所有边，构建邻接表。 |
| **初始化堆** | 将起点 1 的边入堆 | O(D) | D 为起点 1 的度数，通常远小于 M。 |
| **弹出最小边** | `heap.top()` / `heap.pop()` | O(log M) | 每次弹出堆顶（最小边），堆调整耗时 O(log M)。 |
| **遍历邻接边** | `for (auto& e : graph[next])` | O(M) | 所有节点被选中后，其所有的邻边一共会被遍历一次。 |
| **入堆** | `heap.push(...)` | O(log M) | 每一条边最多可能被压入堆中一次。 |

#### 最终结论（大 O 表示法）：
*   **总时间复杂度：O(M log M)**
    *(其中 M 是图的边数。因为每条边可能会进堆、出堆一次，每次堆操作是 O(log M)。)*
*   实际中因为堆的大小最多为 M，所以常写作 **O(M log M)**。
*   **补充：** 如果图是稠密图（M 接近 N^2），Prim 堆优化版的时间复杂度近似 **O(N^2 log N)**；如果是稀疏图（M 远小于 N^2），效率极高。

---

### 2. 空间复杂度

| 数据结构 | 代码对应 | 空间复杂度 | 说明 |
| :--- | :--- | :--- | :--- |
| **邻接表** | `vector<vector<pair<int,int>>> graph` | O(N + M) | 存储 N 个节点，以及每个节点连出的所有边（无向图每条边存两次）。 |
| **访问标记** | `vector<bool> set` | O(N) | 记录 N 个节点是否已加入生成树。 |
| **小根堆** | `priority_queue<pair<int,int>> heap` | O(M) | 最坏情况下所有边都会被放入堆中。 |

#### 最终结论（大 O 表示法）：
*   **总空间复杂度：O(N + M)**
    *(临接表本身是 O(N+M)，加上堆和标记数组，总量级不变。)*

---

### 💡 总结与对比
*   堆优化的 Prim 算法非常适合 **稀疏图**（边数较少），此时时间复杂度近乎线性的 O(M log M)。
*   如果你的图是**稠密图**（边非常多），可以直接用**二维数组（邻接矩阵） + 暴力寻找最小边**的 Prim 算法，此时复杂度为 **O(N^2)**，反而比堆优化更快，因为避免了堆操作带来的 log M 常数开销。

## 最小生成树 (MST) 算法大比拼：Kruskal vs Prim

### 1. 核心思想对比

| 对比项 | Kruskal (克鲁斯卡尔) | Prim (普里姆) |
| :--- | :--- | :--- |
| **核心策略** | **加边法**：将所有边按权值从小到大排序，贪心地选边，用并查集判环。 | **加点法**：从一个点开始，不断寻找连接“已选集合”与“未选集合”的权值最小边，将新点加入集合。 |
| **依赖的数据结构** | **并查集 (DSU)** + 排序 | **小根堆 (优先队列)** + 标记数组 |
| **切入点** | 全局考虑，看的是**边**。 | 局部扩张，看的是**点**。 |
| **边的处理** | 边只要不成环，就选进来。 | 只会选连接集合内外的边，自动避免环。 |

---

### 2. 时间复杂度对比（基于你的模板）

| 算法 | 适用场景 | 核心操作 | 时间复杂度 | 空间复杂度 |
| :--- | :--- | :--- | :--- | :--- |
| **Kruskal** | **稀疏图** (边少) | 对 M 条边排序 | **O(M log M)** | **O(N + M)** |
| **Prim (堆优化)** | **稠密图** (边多) | 遍历 N 个点，每次操作堆 | **O(M log M)** | **O(N + M)** |
| **Prim (无堆/暴力)** | **稠密图** (边极多) | 暴力找最小边 | **O(N^2)** | **O(N^2)** |

---

### 3. 优缺点深度解析

#### 🔵 Kruskal 算法
*   **优点**：
  1. 逻辑极其简单，只要会排序和并查集就能写出来。
  2. 非常适合**稀疏图**（M 很小），只要把边排好序，跑一遍并查集即可。
  3. 因为只关心边，非常适合处理**多组测试数据**和**全局排序**的问题。
*   **缺点**：
  1. 必须先对 M 条边排序。如果 M 接近 N^2（稠密图），排序本身就要花掉 O(M log M)，性能反而不如暴力 Prim。
  2. 必须额外维护一个并查集。

#### 🟢 Prim 算法
*   **优点**：
  1. 不需要对所有边全局排序，只需要动态维护堆，对于稠密图（M 接近 N^2）效率很高。
  2. 不需要并查集，只需要一个简单的 bool 数组（set）来标记。
*   **缺点**：
  1. 必须建图（邻接表或邻接矩阵）。
  2. 必须有一个明确的起点（虽然任意点都可以，但逻辑上必须从一个点开始逐渐扩张）。
  3. 对于稀疏图，堆操作带来的 log M 常数可能略大。

---

### 4. 实战选择建议（怎么选？）

*   **如果图是稀疏图**（比如 M 和 N 差不多大，像树、公路网）：**无脑选 Kruskal**。因为排序比堆操作快，且代码更好写。
*   **如果图是稠密图**（比如 M 接近 N^2，像完全图、城市间所有路线都连通）：**选 Prim**，特别是**无堆优化版（二维数组版）**，此时 O(N^2) 是最优解。
*   **题目给的是边列表**：绝大多数情况下用 **Kruskal** 最省事。
*   **题目给的是邻接矩阵**：用 **Prim** 最顺手。

---

### 5. 一句话总结

> **Kruskal 是“挑边”的高手（靠并查集防成环），适合稀疏图；**
> **Prim 是“占地盘”的高手（靠堆找最小扩展），适合稠密图。**