# 滑动窗口的学习
* 滑动窗口的是啥:
  * 滑动窗口:维持左,右边界都不回退的一段范围。来求解很多子数组(串)相关的问题
  * 滑动窗口的关键:找到范围和答案指标之间的单调性关系(类似贪心)
  * 滑动过程:滑动窗口可以用简单变量，或者结构来维护信息
  * 滑动窗口的特点:一直往前走不回退
* 关于滑动窗口，其实也没啥好说的，先来两个题目:
## 题目一:洛谷P1567
题目链接:https://www.luogu.com.cn/problem/P1567#ide
这个题目其实很简单，代码如下:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    vector<int> v;//开创一个空间
    for(int i=0;i<n;i++){
        int num;
        cin>>num;
        v.push_back(num);
    }
    int count=1;//初始化天数为1
    int Max=1;//初始化的最大值就是count
    for(int i=0;i<n;i++){
        if(v[i+1]>v[i]){
            count++;//当大于时，我去++天数
        }else{
            count=1;//如果严格递增断了，就重置count
        }
        Max=max(Max,count);//更新我们的最大天数
    }
    cout<<Max;
    return 0;
}
```
在这道题目里面其实已经有窗口的概念了，它是利用满足后一个大于前一个的条件count++,然后如果严格递增断了，就重置count， 就要开启一个新的窗口了，这就是这个题目的核心解法
## 题目二洛谷P1614
题目链接:https://www.luogu.com.cn/problem/P1614
还是一样，先看代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main(){
    int n,m;
    cin>>n>>m;
    vector<int> v;//开容器储存
    for(int i=0;i<n;i++){
        int num;
        cin>>num;
        v.push_back(num);
    }
    int current=0;
    for(int i=0;i<m;i++){
        current+=v[i];//我们可以现标记前0~m-1个数的和
    }
    int min=current;//将最小值暂定为它
    for(int i=m;i<n;i++){
        current=current-v[i-m]+v[i];//在前面吐出一个元素，向后面容纳进一个元素，相当于我们的窗口在向后移动。
        if(current<min){
            min=current;//不断更新最小值
        }
    }
    cout<<min<<endl;
    return 0;
}
```
这个题目其实就更能体现滑动窗口思想了，我们要看数组里一段长为`m`的子数组和最小组，我们就可以先取数组前m个元素的和，然后再把窗口向右移，在前面吐出一个元素，向后面容纳进一个元素，相当于我们的窗口在向后移动。
然后不断更新我们的最小值，这道题目就可以直接秒杀了

* 基于这里，我们就可以真正意义上进入滑动窗口的题目了:
## 题目三 力扣209
题目链接:https://leetcode.cn/problems/minimum-size-subarray-sum/submissions/727331007/

首先，对于这个题目，我们知道，我们可以我们要形成一个窗口，可以利用双指针，定义一个`left`和`right`指针，让右指针一直右移，左指针先不动
然后累加当前左右指针围出来的和看看他是否比`target`大，如果大于了，那我们就要从左边吐出一个元素，再次判断，在所有和小于`target`的区间里寻找最短的就可以了
所以代码如下:
```cpp
class Solution {
public:
    int minSubArrayLen(int target, vector<int>& nums) {
        int sum = 0;           // 当前滑动窗口内所有元素的和
        int left = 0;          // 滑动窗口的左边界（索引）
        int MinLen = INT_MAX;  // 记录满足条件的最短子数组长度，初始设为最大整数
        // right 是滑动窗口的右边界，从左向右不断扩展窗口
        for (int right = 0; right < nums.size(); right++) {
            sum += nums[right]; // 将新元素加入窗口
            // 【贪心核心：尝试收缩左边界】
            // 只要当前窗口的和减去左边界的元素，依然 >= target，
            // 说明我们现在的窗口太长了，可以把左边界往右挪，让长度更短
            while (sum - nums[left] >= target) {
                sum -= nums[left]; // 从窗口和中减去左边界元素
                left++;            // 左指针右移，缩短窗口
            }
            // 【更新答案】
            // 经过上面的收缩后，如果 sum 依然 >= target，
            // 说明当前窗口是“刚好满足”且“不能再缩短”的最优窗口，此时更新最短长度
            if (sum >= target) {
                MinLen = min(MinLen, right - left + 1);
            }
        }

        // 如果 MinLen 还是初始的 INT_MAX，说明整个数组都凑不出满足条件的子数组，返回 0
        return MinLen == INT_MAX ? 0 : MinLen;
    }
};
```
### 题目四
* 再看一个作业题目:

#### 6-1 组队

#### 题目描述
 
你的团队中有 $n$ 个人，每个人有一个能力值 $ai$，现在需要选择若干个人组成一个团队去参加比赛，由于比赛的规则限制，一个团队里面任意两个人能力的差值必须要小于等于 $k$ ，为了让更多的人有参加比赛的机会，你最多能选择多少个人参加比赛？

#### 输入格式
 
第一行一个整数 $T$，表示案例组数。

每个案例有两行：

第一行两个正整数 $n$,$k$ ，表示人的数量。

第二行$n$个以空格分隔的整数 $ai$ ，表示每个人的能力值。

#### 输出格式
 
每个案例输出一行，表示可以参加比赛的**最多人数**。

#### 输入样例
 
在这里给出一组输入。例如：
#### 输入样例
在这里给出一组输入。例如：
```in
1
5 3
8 3 5 1 6
```
#### 输出样例
在这里给出相应的输出。例如：
```out
3
```
#### 说明
选择能力值为**3,5,6** 或者**5,6,8**

对于这个题目最关键的一句话就在:一个团队里面任意两个人能力的差值必须要小于等于 $k$
那我们如何让任意两个人的能力差值小于等于$k$呢，其实很简单，只要最大值减去最小值的差值小于等于$k$即可
所以我们就是要从原数组里选出一个窗口子数组，满足最大值和最小值的差值都小于等于$k$
那我们又如何得到最大值和最小值呢，其实只要排个序就可以了，所以代码如下:
```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int T; // 测试用例组数
    cin >> T;
    while (T--) {
        int n, k;
        cin >> n >> k; // n：团队人数；k：能力差值上限
        vector<int> a(n);
        for (int i = 0; i < n; i++) cin >> a[i];
        // 【第一步：排序】
        // 由于团队中“任意两人能力差值 <= k”，等价于“最大值 - 最小值 <= k”
        // 所以先将数组排序，方便后续用滑动窗口寻找合法区间
        sort(a.begin(), a.end());
        int left = 0;    // 滑动窗口的左边界（当前团队中能力最低的人）
        int maxlen = 0;  // 记录满足条件的最大团队人数
        // right 是滑动窗口的右边界（当前团队中能力最高的人）
        for (int right = 0; right < n; right++) {
            // 【第二步：收缩窗口】
            // 如果当前最强者 a[right] 和最弱者 a[left] 的差值 > k，
            // 说明这个团队不合法，必须把最弱者踢出团队（left 右移）
            while (a[right] - a[left] > k) {
                left++;
            }
            // 【第三步：更新答案】
            // 收缩完成后，窗口 [left, right] 内的所有人能力差值都 <= k
            // 此时团队人数为 right - left + 1，尝试更新最大值
            maxlen = max(maxlen, right - left + 1);
        }
        cout << maxlen << endl;
    }
    return 0;
}
```
### 题目五:力扣无重复字符的最长子串
题目链接：https://leetcode.cn/problems/longest-substring-without-repeating-characters/description/

#### 一、核心思路

对于这个题目，我们知道，我们要重视的是边界的移动中的选取，其实我们在观察中：

**左边界 = max(原来的左边界, 该字符上一次出现的位置 + 1)**

然后记录以当前坐标为右边界的最长不重复窗口长度。
在多次记录中还选一个最长的即为最终的答案了。

#### 为什么左边界要取 max？
假设字符串是 `abba`：
1. 当右指针走到第 2 个 `b`（下标 2）时，`b` 上次出现在下标 1，所以左边界要跳到 2。
2. 当右指针走到第 2 个 `a`（下标 3）时，`a` 上次出现在下标 0。如果直接取 `0 + 1 = 1`，那左边界就倒退了！但实际上我们刚才已经走到下标 2 了，不能倒退。
3. 所以必须取 `max(原来的左边界, 上次出现位置 + 1)`，保证左边界只会向右移动，绝不后退。

#### 二、图解模拟（以 `abcabcbb` 为例）

| 右指针 right | 当前字符 | 上次出现位置 | 左边界 left | 窗口 | 长度 |
| :---: | :---: | :---: | :---: | :--- | :---: |
| 0 | a | -1 | 0 | [a] | 1 |
| 1 | b | -1 | 0 | [a,b] | 2 |
| 2 | c | -1 | 0 | [a,b,c] | 3 |
| 3 | a | 0 | max(0, 0+1) = 1 | [b,c,a] | 3 |
| 4 | b | 1 | max(1, 1+1) = 2 | [c,a,b] | 3 |
| 5 | c | 2 | max(2, 2+1) = 3 | [a,b,c] | 3 |
| 6 | b | 4 | max(3, 4+1) = 5 | [c,b] | 2 |
| 7 | b | 6 | max(5, 6+1) = 7 | [b] | 1 |

最终答案：**3**（`abc` 或 `bca` 或 `cab`）
所以代码如下:
```cpp
class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        unordered_set<char> st; // 哈希集合：存放当前窗口中的所有字符，用于 O(1) 判断是否重复
        int maxLen = 0;         // 记录最长无重复子串的长度
        int left = 0;           // 滑动窗口的左边界
        // i 是滑动窗口的右边界，从左向右扩展
        for (int i = 0; i < s.size(); ++i) {
            // 【核心收缩逻辑】
            // 如果当前字符 s[i] 已经在窗口集合 st 中出现了，
            // 说明窗口内有重复，必须从左侧逐个移出字符，直到 s[i] 在窗口里不再重复
            while (st.count(s[i])) {
                st.erase(s[left]); // 把最左侧的字符从集合中删除
                left++;            // 左边界右移
            }
            // 此时窗口里已经没有 s[i] 的重复了，安全插入
            st.insert(s[i]);
            // 更新答案：当前窗口长度是 i - left + 1
            maxLen = max(maxLen, i - left + 1);
        }
        return maxLen;
    }
};
```

