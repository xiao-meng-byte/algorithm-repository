# 质数筛的学习
## 问题三：求质数问题

还是看上个学期的一道**作业题**：

## 6-6

### 题目描述
编程求出大于 m 的最小素数。

### 输入格式
直接输入一个正整数

### 输出格式
直接输出结果，行末无换行

### 输入样例
在这里给出一组输入。例如：
```text
12
```

### 输出样例
在这里给出相应的输出。例如：
```text
13
```

### 代码如下：

```c
#include <stdio.h>
int isPrime(int n) {
    // 小于等于1的数一定不是素数
    if (n <= 1) return 0;
    // 2 是最小的素数，也是唯一的偶素数
    else if (n == 2) return 1;
    // 大于2的偶数一定不是素数（能被2整除）
    else if (n % 2 == 0) return 0;
    // 剩下的都是奇数，只需判断从 3 开始 到 sqrt(n) 的奇数能否整除 n
    // 因为因子都是成对出现的，所以只需检查到 i*i <= n
    else for (int i = 3; i * i <= n; i += 2) {
        // 如果能被整除，说明有因子，不是素数
        if (n % i == 0) return 0;
    }
    // 所有可能的因子都试过了，都不能整除，说明是素数
    return 1;
}
int main() {
    int m;
    scanf("%d",&m);
    for (int i=2;;i++) {
        if (isPrime(i)&&i>m) {
            printf("%d",i);
            break;
        }
    }
    return 0;
}
```

其实求质数有多种方法：

---

## ① 试除法

```c
int isPrime(int n) {
    if (n<=1) return 0;
    else if (n==2) return 1;
    else if (n%2==0) return 0;
    else for (int i=3;i*i<=n;i+=2) {
        if (n%i==0) return 0;
    }
    return 1;
}
```

---

## ② 埃式筛法

它是靠啥来筛？就是说我们先把 `2~n` 间的所有数都视为质数，那我们已经知道 `2` 是个质数，那我们就可以说它的倍数就一定不是质数，接着不断遍历，一样的方法来比如我已经筛出 `3` 是质数，那 `6，9，12.....` 等都不是质数，最后就可得到答案。

```c
#define MAX 100005
int prime[MAX];
void sieve(int n){
    // 初始化：先将所有数标记为 1，表示默认是素数
    for (int i = 2; i <= n; i++) {
        prime[i] = 1;
    }
    // 从 2 开始枚举，筛掉素数的所有倍数
    for (int i = 2; i * i <= n; i++) {//为啥是i*i<=n，因为比如说我n=20，那我如果要验证大于4的数的话，6，9，12它们一定有个因子在2~n^(1/2)之间，所以没必要验了
        // 如果当前数 i 是素数
        if (prime[i] == 1) {
            // 将 i 的所有倍数标记为非素数
            for (int j = i * 2; j <= n; j += i) {
                prime[j] = 0;
            }
        }
    }
}
```

> **缺点**：埃式筛会有重复验证的情况，就是一个数可能会被验多次，比如 `2` 的倍数有 `4，6` 等等，这些数在后面遍历的时候还会被验证，所以它的时间复杂度偏慢，为 `O(n log log n)`，不如欧拉筛。

---

## ③ 欧拉筛（线性筛）

欧拉筛靠啥只对每个数筛一次？我们知道埃式筛慢的原因是一个数被多个因数验证，就是比如 `12`，会被 `2, 3` 等都验证一次，那我怎么只筛一次呢？其实我们可以用前面的找到的质数去验证后面的数，比如说我知道 `2` 是质数，那我就去看当前这个数 × `2` 是不是合数，如果是，就标记一次，然后立刻停止，不让更大的质数再来标记。

```c
#define MAX 1000005
// isPrime[i] = 1 表示 i 是素数，0 表示合数
int isPrime[MAX];
// 存储所有筛出来的素数
int prime[MAX];
// 记录已经筛出多少个素数
int prime_count;    
void euler_sieve(int n) {
    // 初始化：先把所有数暂时标记为素数
    for (int i = 2; i <= n; i++) {
        isPrime[i] = 1;
    }
    // 初始时素数个数为 0
    prime_count = 0;
    // 遍历 2~n，逐个判断与筛除
    for (int i = 2; i <= n; i++) {
        //如果i没被筛掉，说明是素数，那就加入素数数组
        if (isPrime[i]) {
            prime[prime_count++] = i;
        }
        for (int j = 0; j < prime_count; j++) {
            int p = prime[j];  // 当前素数 p
            // 防止越界：i*p 超过 n 就不用筛了
            if ((long long)i * p > n) {
                break;
            }
            // i*p是合数，标记为非素数
            isPrime[i * p] = 0;
            // 保证每个合数只被最小质因子筛一次
            // 就是说如果p是i的因子，那么i后面的素数更大，会造成重复，直接 break
            if (i % p == 0) {
                break;
            }
        }
    }
}
```
## 埃氏筛与欧拉筛深度比较

### 1. 核心原理与执行逻辑
* **埃氏筛**：采用**“倍数消除法”**。当确认一个数是质数时，直接将其所有的整数倍（2倍、3倍、4倍...）全部标记为非质数。这是一种“向前扫射”的策略。
* **欧拉筛**：采用**“最小质因子标记法”**。从小到大遍历所有数字，用已经筛选出的质数去乘当前数字。一旦当前数字能被某个质数整除，立即停止后续质数的运算（break）。这是一种“精准打击”的策略。

### 2. 时间复杂度
* **埃氏筛**：**O(n log log n)**。由于存在冗余操作（同一个合数会被多个不同质因子重复标记），实际运行效率略低于线性。
* **欧拉筛**：**O(n)**。每个合数只被它的“最小质因子”标记一次，所有数字只被访问一次，是真正的线性时间复杂度。

### 3. 空间复杂度
* 两种算法均需要开一个长度为 n 的布尔数组来标记质数/合数，空间复杂度均为 **O(n)**。
* 欧拉筛需要额外一个数组存储已发现的质数列表，但大小远小于 n，空间开销几乎可以忽略。

### 4. 核心优势与适用场景
* **埃氏筛**：代码逻辑极其简单，容易记忆和手写。适用于数据范围较小（如 n <= 10^6）的题目，也常用于快速求得一段区间内的质数。
* **欧拉筛**：效率极高，适用于数据范围极大（如 n <= 10^7 或更大）且对时间要求极其严格的题目。此外，由于它记录了每个合数被“最小质因子”标记的过程，非常适合用来**同时求解区间内每个数的最小质因子**、**欧拉函数**等附加信息。

### 5. 核心思想总结
* **埃氏筛**：是“暴力枚举”，思想是“只要我是质数，我的倍数全是合数”。
* **欧拉筛**：是“贪心+剪枝”，思想是“每个合数只让它的最小质因子去标记，避免重复劳动”。

## 好题
### 题目一:
### U148828 素数判断(Miller-Rabin模板)
题目链接:https://www.luogu.com.cn/problem/U148828#ide
#### 题目描述

给定 $n$ 以及 $n$ 个正整数，请你判断这 $n$ 个正整数是否为质数，对于第 $i$ 个正整数，如果它是质数输出"Yes" ，否则输出 "No" (不含引号)。

#### 输入格式

第一行一个整数:$n$ ($n <= 10000$)

第 $2$ 到第 $n + 1$ 行，每行一个整数 $x$，表示需要判断的正整数。

#### 输出格式

一共 $i$ 行。

对于第 $i$ 个正整数，如果它是质数在第 $i$ 行输出"Yes" ，否则输出 "No" (不含引号)。

#### 输入输出样例 #1

#### 输入 #1

```in
5
1
2
3
4
5
```

#### 输出 #1

```out
No
Yes
Yes
No
Yes
```

## 说明/提示

$30$% 的数据保证: $n <= 7000$ 并且 $x <= 10^9$

$100$% 的数据保证: $n <= 10000$ 并且 $x <= 10^{18}$

(具体数据会根据 $x$ 的值进行调整，保证常数正常的 $Miller Rabin$ 算法可以通过本题)

数据已经放入了 [卡米切尔数](https://baike.baidu.com/item/%E5%8D%A1%E8%BF%88%E5%85%8B%E5%B0%94%E6%95%B0/9083263?fr=aladdin&fromid=100223&fromtitle=%E5%8D%A1%E7%B1%B3%E5%88%87%E5%B0%94%E6%95%B0)，请勿试图用费马小定理过掉本题。

### 思考
首先，对于这个题目**java**是可以直接调库:
```java
import java.io.*;
import java.math.BigInteger;
public class Main {
	// 测试次数，次数越多失误率越低，但速度也越慢
	public static int s = 10;
	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		PrintWriter out = new PrintWriter(new OutputStreamWriter(System.out));
		int t = Integer.valueOf(br.readLine());
		for (int i = 0; i < t; i++) {
			BigInteger n = new BigInteger(br.readLine());
			// isProbablePrime方法包含MillerRabin和LucasLehmer测试
			// 给定测试次数s即可
			out.println(n.isProbablePrime(s) ? "Yes" : "No");
		}
		out.flush();
		out.close();
		br.close();
	}
}
```
如果是**cpp**的写法，我们就需要明白底层原理了:
这是为你整理的**详细版解题思路与代码注释的完整 Markdown 源码**。你可以直接复制粘贴到你的笔记软件（Typora、Obsidian 或 Gitee/GitHub）中：

## 大数质数判定：Miller-Rabin 素性测试（详细版）

### 一、前置知识：为什么普通算法不行？

如果输入的数字 n 在 10^18 级别（比如 1,000,000,000,000,000,000）：

1. **试除法**：你需要从 2 试除到 sqrt(n) ≈ 10^9。这意味着你需要循环 10 亿次，绝对超时（TLE）。
2. **Java 的 BigInteger**：在 C++ 里没有内置的 BigInt，而且它的实现底层依然很慢。
3. **费马小定理**：如果 p 是质数，那么对于任意整数 a，都有 a^(p-1) ≡ 1 (mod p)。
4. 但存在一些"伪装者"（卡迈克尔数，比如 561），它们不是质数，却能骗过费马测试。

**Miller-Rabin 算法** 就是费马小定理的**加强版**，它加入了"二次探测"，专门用来干掉这些"伪装者"。

---

### 二、代码逐行深层解析

#### 1. 为什么必须有 `__int128` 的快速乘？

```cpp
ll mul(ll a, ll b, ll mod) {
    return (__int128)a * b % mod;
}
```

- **问题**：在 64 位系统下，`long long` 最大大约是 9 * 10^18。但 n 如果取到了 10^18，两个小于 10^18 的数相乘，结果可能是 10^36，直接撑爆 `long long`，发生溢出。
- **原理**：`__int128` 是 GCC 编译器特有的 128 位大整数，最大能存到 1.7 * 10^38，完全能兜得住。我们用它做中间运算，取模后再塞回 `long long`。
- **没有它会怎样？** 程序会算出错误的模值，导致误判质数。

#### 2. 快速幂是怎么提速的？

```cpp
ll qpow(ll a, ll b, ll mod) {
    ll res = 1;
    while (b) {
        if (b & 1) res = mul(res, a, mod);
        a = mul(a, a, mod);
        b >>= 1;
    }
    return res;
}
```

- **原理**：我们要算 a^b (mod n)。如果直接循环乘 b 次，复杂度是 O(b)，而 b 可能接近 10^18，会超时。
- **优化**：把 b 转换成二进制。例如算 a^13，13 的二进制是 1101。我们只需要算 a^1, a^2, a^4, a^8（每次平方），然后把对应的项乘起来。复杂度降到了 O(log b)，也就是最多 60 次循环，瞬间完成。

#### 3. 为什么要把 n-1 拆成 d * 2^r 的形式？

```cpp
ll d = n - 1;
int r = 0;
while ((d & 1) == 0) {
    d >>= 1;
    r++;
}
```

- **数学原理**：因为 n 是质数且是奇数（偶数被特判排除了），所以 n-1 一定是偶数。为了使用"二次探测"，必须把 n-1 的所有因子 2 提出来，写成 n-1 = d * 2^r。
- **二次探测公式**：令 x = a^d (mod n)。
    - 如果 x = 1 或 x = n-1，可能是质数，继续测下一个基底。
    - 如果都不满足，就把 x 不断平方（最多平方 r-1 次）。如果在某次平方后 x = n-1，说明通过了，可能是质数。
    - 如果平方了 r-1 次还没有遇到 n-1，那这绝对是合数。

#### 4. 为什么是这 7 个固定的数字？

```cpp
int bases[] = {2, 325, 9375, 28178, 450775, 9780504, 1795265022};
```

- 对于一般的 n，如果随机选 a，Miller-Rabin 是概率性算法（极小概率出错）。
- 但是，数学家们通过穷举计算证明了：在 64 位无符号整数范围内，只要用上面这 7 个数字作为基底，测试就是确定性的、100% 不会出错的。如果你背下来了这 7 个数，就相当于拿到了 Miller-Rabin 的最强"外挂"。

#### 5. 为什么会有 `composite` 这个标记？

```cpp
bool composite = true; // 假设 n 是合数
for (int j = 0; j < r - 1; j++) {
    x = mul(x, x, n);
    if (x == n - 1) {
        composite = false; // 遇到了 n-1，说明通过了测试
        break;
    }
}
if (composite) return false;
```

- 我们初始化 `composite = true`（视为合数）。
- 在平方探测的过程中，只要一旦发生 x = n-1，我们就把标记改为 `false`（不是合数），并立刻跳出内层循环。
- 如果循环走完了，`composite` 还是 `true`，说明从头到尾都没能变出 n-1，这就是货真价实的合数，立刻返回 `false`。

---

### 三、举个具象化的例子（模拟测试）

假设我们要测试 n = 9 是不是质数：

1. 因为 9 > 2，且不是偶数。
2. 9-1 = 8，把 8 拆成 8 = 1 * 2^3，所以 d=1, r=3。
3. 取第一个基底 a = 2。
4. 算 x = 2^1 (mod 9) = 2。
5. 因为 x 不是 1 也不是 8（即 n-1），所以进入二次探测。
6. 第一次平方：x = 2^2 (mod 9) = 4（不是8，继续）。
7. 第二次平方：x = 4^2 (mod 9) = 7（不是8，继续）。
8. 由于探测次数用完了，还是没碰到 8，所以 `composite = true`，判定 9 是合数，返回 `false`。判定正确！

---

### 四、完整代码（带详细注释）

```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

// 快速乘：防止 (a * b) % mod 溢出 ll
// 利用 __int128 进行中间计算，最后取模。
ll mul(ll a, ll b, ll mod) {
    return (__int128)a * b % mod;
}

// 快速幂：计算 (a^b) % mod
// 利用二进制拆分 b，将 O(b) 的复杂度降到 O(log b)
ll qpow(ll a, ll b, ll mod) {
    ll res = 1;
    while (b) {
        if (b & 1) res = mul(res, a, mod); // 如果当前位是1，乘上 a
        a = mul(a, a, mod);                 // a 每次自己平方
        b >>= 1;                            // b 右移一位
    }
    return res;
}

// 判断 n 是否是素数（确定性 Miller-Rabin 基底，适用于 64 位整数）
bool isPrime(ll n) {
    // 1. 处理小数据特判
    if (n < 2) return false;      // 小于2不是素数
    if (n == 2 || n == 3) return true; // 2和3是素数
    if (n % 2 == 0) return false; // 大于2的偶数不是素数

    // 2. 将 n-1 分解为 d * 2^r 的形式
    // n-1 是偶数，可以先提出所有的因子2，剩下奇数 d
    ll d = n - 1;
    int r = 0;
    while ((d & 1) == 0) {
        d >>= 1;  // 不断除以2
        r++;      // 记录2的幂次
    }

    // 3. 使用确定的7个基底进行测试
    // 这些数字是经过数学证明的，对于64位整数可以100%判定正确
    int bases[] = {2, 325, 9375, 28178, 450775, 9780504, 1795265022};
    for (int i = 0; i < 7; i++) {
        ll a = bases[i] % n; 
        if (a == 0) continue; // 如果基底是 n 的倍数，测试无意义，跳过

        // 计算 a^d % n
        ll x = qpow(a, d, n);
        
        // 如果 x == 1 或 x == n-1，说明通过了当前基底的测试，继续测下一个
        if (x == 1 || x == n - 1) continue;

        // 4. 二次探测：不断将 x 平方，看能不能得到 n-1
        bool composite = true; // 假设 n 是合数（复合数）
        for (int j = 0; j < r - 1; j++) {
            x = mul(x, x, n); // x = x^2 % n
            if (x == n - 1) {
                composite = false; // 遇到了 n-1，说明通过了测试
                break;
            }
        }
        
        // 如果经过了所有平方探测，都没有得到 n-1，说明 n 真的是合数
        if (composite) return false;
    }
    
    // 7个基底全部通过，说明 n 是素数
    return true;
}

int main() {
    // 开启 C++ 快速 IO，防止大数据下超时
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int t; // 测试数据组数
    cin >> t;
    while (t--) {
        ll n;
        cin >> n;
        // 输出判定结果
        cout << (isPrime(n) ? "Yes" : "No") << "\n";
    }
    return 0;
}
```

---

### 五、总结

这个模板是 64 位整数下判定大数质数的**最强兵器**：

- `__int128` 解决了溢出问题。
- `快速幂` 解决了复杂度问题。
- `7 个固定基底 + 二次探测` 解决了准确率问题。
