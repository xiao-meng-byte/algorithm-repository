## 高斯消元法
比如以下的矩阵：

$$
\begin{cases}
x_1 + 2x_2 - x_3 = 9 \\
2x_1 - x_2 + 2x_3 = 7 \\
x_1 - 2x_2 + 2x_3 = 0
\end{cases}
$$

**=> 增广矩阵 =>**
$$
\begin{bmatrix} 1 & 2 & -1 & 9 \\ 2 & -1 & 2 & 7 \\ 1 & -2 & 2 & 0 \end{bmatrix}
$$
（右上角标注：$n \times (n+1)$，$n$为元数）

---

### **最终结果（右侧）：**
$$
\begin{aligned}
x_1 &= 4 \\
x_2 &= 3 \\
x_3 &= 1
\end{aligned}
$$

---

### **中间的行变换步骤（矩阵化简过程）：**

**第一大步：**
$$
\begin{bmatrix} 2 & -1 & 2 & 7 \\ 1 & 2 & -1 & 9 \\ 1 & -2 & 2 & 0 \end{bmatrix}
\Rightarrow
\begin{bmatrix} 1 & -\frac{1}{2} & 1 & \frac{7}{2} \\ 1 & 2 & -1 & 9 \\ 1 & -2 & 2 & 0 \end{bmatrix}
\Rightarrow
\begin{bmatrix} 1 & -\frac{1}{2} & 1 & \frac{7}{2} \\ 0 & \frac{5}{2} & -2 & \frac{11}{2} \\ 0 & -\frac{3}{2} & 1 & -\frac{7}{2} \end{bmatrix}
$$

**第二大步：**
$$
\begin{bmatrix} 1 & -\frac{1}{2} & 1 & \frac{7}{2} \\ 0 & \frac{5}{2} & -2 & \frac{11}{2} \\ 0 & -\frac{3}{2} & 1 & -\frac{7}{2} \end{bmatrix}
\Rightarrow
\begin{bmatrix} 1 & 0 & \frac{1}{5} & \frac{26}{5} \\ 0 & 1 & -\frac{4}{5} & \frac{11}{5} \\ 0 & -\frac{3}{2} & 1 & -\frac{7}{2} \end{bmatrix}
\Rightarrow
\begin{bmatrix} 1 & 0 & \frac{1}{5} & \frac{26}{5} \\ 0 & 1 & -\frac{4}{5} & \frac{11}{5} \\ 0 & 0 & -\frac{1}{5} & -\frac{1}{5} \end{bmatrix}
$$
*(注：此处手写数字可能比较潦草，部分数值根据上下文和最终结果推断，如 $\frac{26}{5}$ 的手写看起来略像 $\frac{92}{20}$ 但逻辑上应为 $\frac{26}{5}$)*

**第三大步：**
$$
\begin{bmatrix} 1 & 0 & \frac{1}{5} & \frac{26}{5} \\ 0 & 1 & -\frac{4}{5} & \frac{11}{5} \\ 0 & 0 & -\frac{1}{5} & -\frac{1}{5} \end{bmatrix}
\Rightarrow
\begin{bmatrix} 1 & 0 & \frac{1}{5} & \frac{26}{5} \\ 0 & 1 & -\frac{4}{5} & \frac{11}{5} \\ 0 & 0 & 1 & 1 \end{bmatrix}
\Rightarrow
\begin{bmatrix} 1 & 0 & 0 & 4 \\ 0 & 1 & 0 & 3 \\ 0 & 0 & 1 & 1 \end{bmatrix}
$$

*(注：图中空心的方框“□”表示被涂黑或书写中断的数字，通常代表高斯消元法中将该列主元化为1的过程，此处已按标准步骤还原为1。)*

### 高斯消元法的模板代码

```cpp
//前置知识---小学学的三元一次方程
//每次选绝对值最大的系数交换上来然后消元,联系线性代数
//时间复杂度O(n^3),那啥时候有唯一解，啥时候有多解，啥时候矛盾?
//假设方程的行列式如下；
//      1
//        1   ==>  系数对角线全为1的就有唯一解
//          1
//      1
//        0    0或不为0,只要有一个为0,那就看它后面的结果如果为0就是多解，不为0就是矛盾
//          1
//高斯消元顾名思义就是找主元，即对角线上为1
#include <bits/stdc++.h>
using namespace std;
// 矩阵最大大小
static int MAXN = 101;
// 增广矩阵，前 n 列为变量系数，第 n+1 列为等号右边的常数
static vector<vector<double>> graph;
// 浮点数精度，小于 sml 的数认为是 0
static double sml = 1e-7;
// 高斯-约旦消元，n 为变量数量和方程数量
void gauss(int n) {
    // 依次处理第 1 列到第 n 列
    for (int i = 1; i <= n; i++) {
        // 记录当前列绝对值最大的元素所在行
        int max = i;
        // 遍历所有尚未确定主元的行，寻找当前列绝对值最大的元素
        for (int j = 1; j <= n; j++) {
            // 前面的行如果已经有主元，就不再参与寻找
            if (j < i && abs(graph[j][j]) >= sml) {
                continue;
            }
            // 更新当前列绝对值最大的元素所在行
            if (abs(graph[j][i]) > abs(graph[max][i])) {
                max = j;
            }
        }
        // 将第 i 行和第 max 行交换，使最大元素成为当前主元
        swap(graph[i], graph[max]);
        // 如果当前主元不为 0
        if (abs(graph[i][i]) >= sml) {
            // 保存主元
            double tmp = graph[i][i];
            // 当前行除以主元，使主元变为 1
            for (int j = i; j <= n + 1; j++) {
                graph[i][j] /= tmp;
            }
            // 利用当前行消去其他所有行的第 i 列
            for (int j = 1; j <= n; j++) {
                // 不需要消除自己
                if (i != j) {
                    // 计算需要减去的倍数
                    double rate = graph[j][i] / graph[i][i];
                    // 第 j 行减去第 i 行乘以 rate
                    for (int k = i; k <= n + 1; k++) {
                        graph[j][k] -= graph[i][k] * rate;
                    }
                }
            }
        }
    }
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // 输入变量数量，同时也是方程数量
    int n;
    cin >> n;
    // 使用下标 1，所以额外开一行一列
    graph.assign(n + 1, vector<double>(n + 2));
    // 输入增广矩阵
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n + 1; j++) {
            cin >> graph[i][j];
        }
    }
    // 进行高斯-约旦消元
    gauss(n);
    // 输出消元后的增广矩阵
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n + 1; j++) {
            // 消除浮点数计算产生的微小误差
            if (abs(graph[i][j]) < sml) {
                graph[i][j] = 0;
            }
            cout << graph[i][j] << ' ';
        }
        cout << '\n';
    }
    return 0;
}
```
### 具体题目
* 题目一：
[高斯消元模版题1](https://www.luogu.com.cn/problem/P3389#ide)

题目的代码：
```cpp
//高斯消元顾名思义就是找主元，即对角线上为1
#include <bits/stdc++.h>
using namespace std;
// 矩阵最大大小
static int MAXN = 101;
// 增广矩阵，前 n 列为变量系数，第 n+1 列为等号右边的常数
static vector<vector<double>> graph;
// 浮点数精度，小于 sml 的数认为是 0
static double sml = 1e-7;
// 高斯-约旦消元，n 为变量数量和方程数量
int gauss(int n) {
    // 依次处理第 1 列到第 n 列
    for (int i = 1; i <= n; i++) {
        // 记录当前列绝对值最大的元素所在行
        int max = i;
        // 遍历所有尚未确定主元的行，寻找当前列绝对值最大的元素
        for (int j = 1; j <= n; j++) {
            // 前面的行如果已经有主元，就不再参与寻找
            if (j < i && abs(graph[j][j]) >= sml) {
                continue;
            }
            // 更新当前列绝对值最大的元素所在行
            if (abs(graph[j][i]) > abs(graph[max][i])) {
                max = j;
            }
        }
        // 将第 i 行和第 max 行交换，使最大元素成为当前主元
        swap(graph[i], graph[max]);
        if (abs(graph[i][i]) < sml) {//只要主元为0了，那直接不用下一步了
            return 0;
        }
        // 如果当前主元不为 0
        if (abs(graph[i][i]) >= sml) {
            // 保存主元
            double tmp = graph[i][i];
            // 当前行除以主元，使主元变为 1
            for (int j = i; j <= n + 1; j++) {
                graph[i][j] /= tmp;
            }
            // 利用当前行消去其他所有行的第 i 列
            for (int j = 1; j <= n; j++) {
                // 不需要消除自己
                if (i != j) {
                    // 计算需要减去的倍数
                    double rate = graph[j][i] / graph[i][i];
                    // 第 j 行减去第 i 行乘以 rate
                    for (int k = i; k <= n + 1; k++) {
                        graph[j][k] -= graph[i][k] * rate;
                    }
                }
            }
        }
    }
    return 1;
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // 输入变量数量，同时也是方程数量
    int n;
    cin >> n;
    // 使用下标 1，所以额外开一行一列
    graph.assign(n + 1, vector<double>(n + 2));
    // 输入增广矩阵
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n + 1; j++) {
            cin >> graph[i][j];
        }
    }
    // 进行高斯-约旦消元
    int num=gauss(n);
    // 输出消元后的增广矩阵
    if (num==0) cout<<"No Solution"<<endl;
    else {
        for (int i = 1; i <= n; i++) {
        cout << fixed << setprecision(2) << graph[i][n + 1] << '\n';
        }
    }
    return 0;
}
```
* 题目二 ：
[高斯消元模板题2](https://www.luogu.com.cn/problem/P2455)

代码如下：
```cpp
//高斯消元顾名思义就是找主元，即对角线上为1
#include <bits/stdc++.h>
using namespace std;
// 矩阵最大大小
static int MAXN = 101;
// 增广矩阵，前 n 列为变量系数，第 n+1 列为等号右边的常数
static vector<vector<double>> graph;
// 浮点数精度，小于 sml 的数认为是 0
static double sml = 1e-7;
// 高斯-约旦消元，n 为变量数量和方程数量
void gauss(int n) {
    // 依次处理第 1 列到第 n 列
    for (int i = 1; i <= n; i++) {
        // 记录当前列绝对值最大的元素所在行
        int max = i;
        // 遍历所有尚未确定主元的行，寻找当前列绝对值最大的元素
        for (int j = 1; j <= n; j++) {
            // 前面的行如果已经有主元，就不再参与寻找
            if (j < i && abs(graph[j][j]) >= sml) {
                continue;
            }
            // 更新当前列绝对值最大的元素所在行
            if (abs(graph[j][i]) > abs(graph[max][i])) {
                max = j;
            }
        }
        // 将第 i 行和第 max 行交换，使最大元素成为当前主元
        swap(graph[i], graph[max]);
        // 如果当前主元不为 0
        if (abs(graph[i][i]) >= sml) {
            // 保存主元
            double tmp = graph[i][i];
            // 当前行除以主元，使主元变为 1
            for (int j = i; j <= n + 1; j++) {
                graph[i][j] /= tmp;
            }
            // 利用当前行消去其他所有行的第 i 列
            for (int j = 1; j <= n; j++) {
                // 不需要消除自己
                if (i != j) {
                    // 计算需要减去的倍数
                    double rate = graph[j][i] / graph[i][i];
                    // 第 j 行减去第 i 行乘以 rate
                    for (int k = i; k <= n + 1; k++) {
                        graph[j][k] -= graph[i][k] * rate;
                    }
                }
            }
        }
    }
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // 输入变量数量，同时也是方程数量
    int n;
    cin >> n;
    // 使用下标 1，所以额外开一行一列
    graph.assign(n + 1, vector<double>(n + 2));
    // 输入增广矩阵
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n + 1; j++) {
            cin >> graph[i][j];
        }
    }
    // 进行高斯-约旦消元
    gauss(n);
    int sign=1;
    for (int i = 1; i <= n; i++) {
        if (abs(graph[i][i]) < sml&&abs(graph[i][n+1]) >=sml) {//这里记录的是无解的情况
            sign=-1;
            break;//这里一定要break不然就会出现和下面重复判断的情况
        }
        if (abs(graph[i][i]) < sml) {
            sign=0;
        }
    }
    if (sign==1) {
        for (int i = 1; i <= n; i++) {
            cout <<"x"<<i<<"="<< fixed << setprecision(2) << graph[i][n + 1] << '\n';
        }
    }else {
        cout<<sign<<endl;
    }
    return 0;
}
```
* 题目三：
[高斯消元进阶题1](https://www.luogu.com.cn/problem/P4035)

对于这个题目的分析
```
这个题目，我们想可以设想，先从二维空间为例，如下图：
              * (a₁,b₁)
             /|\
            / | \
           /  |  \
          /   |   \
         /    |    \
(a₂,b₂) *     |     * (a₃,b₃)
         \    |    /
          \   |   /
           \  |  /
            \ | /
             \|/
              * (x,y)  ← 圆心
              
    三点到圆心距离相等: r，我们在这里很容易可以计算欧式距离：
    (a₁ − x)² + (b₁ − y)² = (a₂ − x)² + (b₂ − y)²
    (a₂ − x)² + (b₂ − y)² = (a₃ − x)² + (b₃ − y)²
    (a₁ − x)² + (b₁ − y)² = (a₃ − x)² + (b₃ − y)²
    那么我们可以化简等到：
    2(a2 - a1)x + 2(b2 - b1)y = a2^2 + b2^2 - a1^2 - b1^2
    2(a3 - a2)x + 2(b3 - b2)y = a3^2 + b3^2 - a2^2 - b2^2
    2(a4 - a3)x + 2(b4 - b3)y = a4^2 + b4^2 - a3^2 - b3^2
    a1,a2,a3,b1,b2,b3是我们已知的，所以这里就和高斯消元就有关系了，这道题就可以迎刃而解了.
```

代码如下：
```cpp
#include <bits/stdc++.h>
using namespace std;
static vector<vector<double>> graph;//建立系数的矩阵
static vector<vector<double>> ans;//建立二维的原数矩阵
static double sml=1e-7;
void gauss(int n) {
    // 依次处理第 1 列到第 n 列
    for (int i = 1; i <= n; i++) {
        // 记录当前列绝对值最大的元素所在行
        int max = i;
        // 遍历所有尚未确定主元的行，寻找当前列绝对值最大的元素
        for (int j = 1; j <= n; j++) {
            // 前面的行如果已经有主元，就不再参与寻找
            if (j < i && abs(graph[j][j]) >= sml) {
                continue;
            }
            // 更新当前列绝对值最大的元素所在行
            if (abs(graph[j][i]) > abs(graph[max][i])) {
                max = j;
            }
        }
        // 将第 i 行和第 max 行交换，使最大元素成为当前主元
        swap(graph[i], graph[max]);
        // 如果当前主元不为 0
        if (abs(graph[i][i]) >= sml) {
            // 保存主元
            double tmp = graph[i][i];
            // 当前行除以主元，使主元变为 1
            for (int j = i; j <= n + 1; j++) {
                graph[i][j] /= tmp;
            }
            // 利用当前行消去其他所有行的第 i 列
            for (int j = 1; j <= n; j++) {
                // 不需要消除自己
                if (i != j) {
                    // 计算需要减去的倍数
                    double rate = graph[j][i] / graph[i][i];
                    // 第 j 行减去第 i 行乘以 rate
                    for (int k = i; k <= n + 1; k++) {
                        graph[j][k] -= graph[i][k] * rate;
                    }
                }
            }
        }
    }
}
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin>>n;
    graph.assign(n+2,vector<double>(n+3));
    ans.assign(n+2,vector<double>(n+3));
    for(int i=1;i<=n+1;i++){
        for(int j=1;j<=n;j++){
            cin>>ans[i][j];
        }
    }
    for(int i=1;i<=n;i++){
        for(int j=1;j<=n;j++){
          graph[i][j]=2*(ans[i][j]-ans[i+1][j]);
          graph[i][n+1]+=ans[i][j]*ans[i][j] -ans[i+1][j]*ans[i+1][j];
        }
    }
    gauss(n);
    for(int i=1;i<=n;i++){
        cout<<fixed<<setprecision(3)<<graph[i][n+1]<<" ";
    }
    return 0;
}
```

* 题目四：
[高斯消元进阶题2](https://www.luogu.com.cn/problem/P5027)


