# 快速幂的学习
## 乘法快速幂
* 首先我们来看代码:
```py
# 乘法快速幂
def quick_pow(a, n, mod):
    res = 1
    a %= mod # 防止溢出
    while n > 0:
        if n & 1:
            res = (res * a) % mod
        a = (a * a) % mod
        n >>= 1
    return res 
a = int(input("请输入底数: "))
b = int(input("请输入指数: "))
mod = int(input("请输入模数: "))
print(quick_pow(a, b, mod))
# 那它的原理是啥呢，就是说我如果能吧我的指数变为二进制，比如说13=2^3+2^2+2^0,那我们在计算a^13次方的时候就可以:
# 把13换为二进制的形式，然后再把a^13转化为a^8*a^4*a^1,这样不需要逐个乘 a，只需不断把底数平方就行了，这样效率更高
# 再看二进制每一位是否为 1，为 1就乘进答案，最后看要不要取模运算，要的话就在乘的时候一起取了。
```
而其实对以上的模板，我们还有更多的一些写法:

* 基于快速幂的二进原理，将乘法替换为加法，计算 `a * n`，时间复杂度 O(log n)。

```py
def quick_mul(a, n, mod):
    res = 0              # 注意：这里是 0，因为做的是加法累加
    while n > 0:
        if n & 1:
            res = (res + a) % mod   # 把乘法换成加法：res += a
        a = (a + a) % mod           # 把平方换成翻倍：a += a
        n >>= 1                     # 指数右移
    return res
```

* 数字快速幂的常见变形

核心思想：**拆二进制，倍增（平方），遇到 1 就乘。**
这里只讨论纯数字/模运算的变式。

---

* 变式 1：标准模板（非负整数指数）

最基础的写法，处理 `a^b % mod`，其中 `b >= 0`。

```py
def quick_pow(a, n, mod):
    if mod == 1: return 0  # 特殊处理：模 1 结果永远是 0
    res = 1
    a %= mod
    while n > 0:
        if n & 1:
            res = (res * a) % mod
        a = (a * a) % mod
        n >>= 1
    return res
```
* **初始值**：`res = 1`
* **注意**：`a %= mod` 先取模，防止刚开始就溢出。

---

* 变式 2：负数指数（转为乘法逆元）

如果 `n` 是负数（比如 `a^(-2)`），在模 `p`（质数）下需要转化为求逆元。
因为 `a^(-n) = (a^(-1))^n`，而逆元是 `a^(p-2)`（费马小定理）。

```py
def quick_pow(a, n, mod):
    if n < 0:
        # 转为求逆元：a^(-n) 等价于 (1/a)^n
        # 费马小定理求逆元：inv(a) = a^(mod-2) % mod
        a = pow(a, mod - 2, mod)  # 用内置函数求逆元
        n = -n  # 把指数变成正数
    return quick_pow(a, n, mod) # 递归或重新调用标准快速幂
```
* **适用条件**：必须保证 `mod` 是质数且 `a` 不是 `mod` 的倍数。

---

* 变式 3：底数为负数（防止负数取模坑）

在 C++ 中，负数取模可能得到负数结果，所以必须保证底数是正数。

```py
def quick_pow(a, n, mod):
    res = 1
    a %= mod
    if a < 0:
        a += mod  # 关键：把负数变正，防止取模出负数
    while n > 0:
        if n & 1:
            res = (res * a) % mod
        a = (a * a) % mod
        n >>= 1
    return res
```
* **易错点**：很多语言（C++）里 `(-5) % 3 = -2`，不统一改成正数会导致结果错误。

---

* 变式 4：底数极大（配合大数 / __int128）

当底数 `a` 和模数 `mod` 都接近 `10^18` 时，`a * a` 会直接爆掉 `long long`（64位）。此时需要配合快速乘。

```cpp
// C++ 版，用快速乘代替乘法
typedef long long ll;

ll mul(ll a, ll b, ll mod) {
    ll res = 0;
    while (b > 0) {
        if (b & 1) res = (res + a) % mod;
        a = (a + a) % mod;
        b >>= 1;
    }
    return res;
}

ll quick_pow(ll a, ll n, ll mod) {
    ll res = 1;
    while (n > 0) {
        if (n & 1) res = mul(res, a, mod);
        a = mul(a, a, mod);
        n >>= 1;
    }
    return res;
}
```
* **场景**：`mod` 特别大，直接乘法会溢出。

---

* 变式 5：指数为 0 或 1（最容易被忽略）

* 当 `n == 0` 时，`quick_pow` 返回 `1`（任何非 0 数的 0 次方为 1）。
* 当 `n == 1` 时，返回 `a % mod`。

```py
def quick_pow(a, n, mod):
    if n == 0:
        return 1 % mod
    if n == 1:
        return a % mod
    res = 1
    a %= mod
    while n > 0:
        if n & 1:
            res = (res * a) % mod
        a = (a * a) % mod
        n >>= 1
    return res
```
* **坑点**：如果题目问 `0^0`，需要根据题意特殊判断（通常规定为 1）。

---

* 💡 数字快速幂变形总结

| 变式 | 核心问题 | 解决方案 |
| :--- | :--- | :--- |
| 标准版 | 计算 `a^n` | 二进制拆分，平方倍增 |
| 负数指数 | 指数为负 | 利用费马小定理转为乘逆元 |
| 负数底数 | 取模溢出/变负 | `a += mod` 保证取模为正 |
| 数字极大 | `a*a` 溢出 | 把 `*` 替换为快速乘（`__int128`） |
| 边界值 | 指数为 0/1 | 加上特判 |


这就是**纯数字快速幂**最常用的几种变式，掌握了它们，在竞赛和平时刷题时遇到各式各样的快速幂题目，都能游刃有余了！

## 矩阵快速幂
### 矩阵快速幂的原理和模板代码
* 首先我们想来看看矩阵怎么乘的:
引用`洛谷B2105`
* 题目链接:https://www.luogu.com.cn/problem/B2105

对于这个题目，我们就要引入线性代数的知识了:
* 我们知道两个矩阵要能相乘我们要满足第一个矩阵的列数要等于第二个矩阵的行数
* 首先我们有两个矩阵:
   * 他们的相乘过程如下:
  
     $$
     \begin{pmatrix}
     a & b \\
     c & d
     \end{pmatrix}
     \times
     \begin{pmatrix}
     e & f \\
     g & h
     \end{pmatrix}
     $$

   * 它们相乘后的结果矩阵结果如下:

$$
\begin{pmatrix}
ae + bg & af + bh \\
ce + dg & cf + dh
\end{pmatrix}
$$

* 根据以上的写法，我们直接用代码实现如下:
```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
static vector<vector<ll>> a;
static vector<vector<ll>> b;
// 矩阵乘法函数：计算 a (n行m列) 乘以 b (m行k列)，结果是 n行k列
vector<vector<ll>> mul(const vector<vector<ll>> &a, const vector<vector<ll>> &b, int n, int m, int k) {
    vector<vector<ll>> res(n, vector<ll>(k, 0)); // 初始化结果矩阵，n行k列，全填0
    for (int i = 0; i < n; i++) {           // 遍历左矩阵的每一行 i
        for (int kk = 0; kk < m; kk++) {    // 遍历左矩阵的每一列（即右矩阵的行） kk
            if (a[i][kk] == 0) continue;    // 剪枝优化：如果当前元素是0，直接跳过，加速运算
            for (int j = 0; j < k; j++) {   // 遍历右矩阵的每一列 j
                res[i][j] += a[i][kk] * b[kk][j]; // 累加相乘的结果
            }
        }
    }
    return res;
}
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr); 
    int n, m, k;
    cin >> n >> m >> k; // 输入左矩阵的行数n，列数m，右矩阵的列数k 
    // 初始化并读入左矩阵 a (n 行 m 列)
    a.assign(n, vector<ll>(m));
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            cin >> a[i][j];
        }
    }  
    // 初始化并读入右矩阵 b (m 行 k 列)
    b.assign(m, vector<ll>(k));
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < k; j++) {
            cin >> b[i][j];
        }
    }  
    // 执行矩阵乘法
    vector<vector<ll>> res = mul(a, b, n, m, k);  
    // 输出结果矩阵 (n 行 k 列)
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < k; j++) {
            cout << res[i][j] << " ";
        }
        cout << endl;
    }    
    return 0;
}
```
* 那矩阵快速幂是啥呢:
  * 首先矩阵快速幂里的矩阵必须是**正方形**，那如果我们要把一个矩阵和数字一样乘出次方呢
  * 那对应上面的`ll ans=1`来初始化，那矩阵里初始化要怎么表示呢:
  其实直接单位矩阵就行了,二阶的话就是这样的:
    $$
    \begin{pmatrix}
    1 & 0 \\
    0 & 1
    \end{pmatrix}
    $$
  * 那其实对比**乘法快速幂**其实对于矩阵的乘法，幂数也可以变为二进制
  * 和乘法快速幂一样可以变为幂数自乘的方式，但是有一个要注意的点:
  * 就是要矩阵乘法的顺序不能变的，学过线代的就知道了。
  * 函数代码如下:
  ```cpp
  vector<vector<ll>> power(vector<vector<ll>> m,int p){
    int n=m.size();
    vector<vector<ll>> ans;
    ans.assign(n, vector<ll>(n));
    for(int i=0;i<n;i++){
        ans[i][i]=1;//把对角线元素设为1，视为单位矩阵
    }
    while(p!=0){
        if((p&1)!=0) ans=mul(ans,m);//这个ans和m的顺序是不能变的
        m=mul(m,m);
        p>>=1;
    }
    return ans;
  }
  ```
 * 搭配上面的`mul`函数就构成了矩阵快速幂。
 * 标准的矩阵快速幂代码如下,题目链接:https://www.luogu.com.cn/problem/P3390#ide
```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const ll MOD = 1000000007; // 题目要求的模数
static vector<vector<ll>> a;
// 方阵矩阵乘法：计算 n x n 矩阵 a 乘以 n x n 矩阵 b
vector<vector<ll>> mul(const vector<vector<ll>> &a, const vector<vector<ll>> &b, int n) {
    vector<vector<ll>> res(n, vector<ll>(n, 0)); // 初始化结果矩阵
    for (int i = 0; i < n; i++) {
        for (int h = 0; h < n; h++) {
            if (a[i][h] == 0) continue; // 剪枝优化
            for (int j = 0; j < n; j++) {
                res[i][j] = (res[i][j] + a[i][h] * b[h][j]) % MOD; // 乘完取模防溢出
            }
        }
    }
    return res;
}
// 矩阵快速幂：计算方阵 m 的 k 次方
vector<vector<ll>> power(vector<vector<ll>> m, ll k, int n) {
    vector<vector<ll>> ans;
    ans.assign(n, vector<ll>(n, 0));
    for (int i = 0; i < n; i++) {
        ans[i][i] = 1; // 单位矩阵
    }
    while (k != 0) {
        if ((k & 1) != 0) ans = mul(ans, m, n); // ans 和 m 的顺序不能变
        m = mul(m, m, n); // 底数自乘
        k >>= 1;
    }
    return ans;
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int n;
    ll k;
    cin >> n >> k; 
    a.assign(n, vector<ll>(n));
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            cin >> a[i][j];
        }
    }
    // 调用矩阵快速幂
    vector<vector<ll>> ans = power(a, k, n);
    // 输出结果矩阵（已经取过模）
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            cout << ans[i][j] << " ";
        }
        cout << endl;
    }
    return 0;
}
```
### 矩阵快速幂（P3390 模板）复杂度分析

#### 1. 时间复杂度

总时间复杂度由**矩阵乘法**的次数和**单次矩阵乘法的开销**决定。

| 步骤 | 代码对应 | 单次/总复杂度 | 说明 |
| :--- | :--- | :--- | :--- |
| **单次矩阵乘法** | `mul(a, b, n)` | **O(n^3)** | 三层循环 `i -> h -> j`，计算矩阵乘法的标准时间复杂度。 |
| **快速幂循环** | `while (k != 0)` | **O(log k)** | 将指数 k 二进制拆分，循环次数等于 k 的二进制位数。 |
| **总时间复杂度** | `power(a, k, n)` | **O(n^3 * log k)** | 每次循环最多执行两次 `mul`，所以总复杂度为 \(O(n^3 \log k)\)。 |

##### 总结（大 O 表示法）：

| 场景 | 时间复杂度 |
| :--- | :--- |
| **总体复杂度** | **O(n^3 * log k)** |
| **最坏情况** | O(n^3 * log k) |
| **实际运行效率** | 题目中 n <= 100，k <= 10^12,配合剪枝优化能在 1 秒内轻松跑完。 |

---

#### 2. 空间复杂度

空间复杂度主要由存储矩阵的容器和临时变量决定。

| 数据结构 | 代码对应 | 空间复杂度 | 说明 |
| :--- | :--- | :--- | :--- |
| **输入矩阵** | `vector<vector<ll>> a` | **O(n^2)** | 用于存储输入的方阵。 |
| **结果矩阵** | `vector<vector<ll>> res` / `ans` | **O(n^2)** | `mul` 函数内和 `power` 函数内各有一个结果矩阵，会暂存计算中间结果。 |
| **临时辅助变量** | `i, j, h, k` | **O(1)** | 循环变量，常量级空间。 |

##### 总结：

| 场景 | 空间复杂度 |
| :--- | :--- |
| **总空间复杂度** | **O(n^2)** |
| **代码运行时峰值** | 大约同时存在 2~3 个 n*n 矩阵，每个矩阵占 \(n^2 \times 8\) 字节，对于 n <= 100 来说非常小。 |

---

### 矩阵快速幂的应用
#### 斐波拉契数列
* 我们知道斐波拉契数列的前几项为:`0,1,1,2,3,5,8,13`.
* 递归关系为fib(n)=fib(n-1)+fib(n-2);
* 我们可以先给出结论: 对于一个斐波拉契数列，他的每次乘的矩阵为:
  $$
  \begin{pmatrix}
  1 & 1 \\
  1 & 0
  \end{pmatrix}
  $$
然后我们用初始矩阵为`[1,0]`,一直乘就可以把后面几项也算出来(比如说乘一次变为`[2,1]`):
* 证明:
#### 证明过程

#### 1. 构造递推等式

设**相邻两项**构成的向量为：
$$
\begin{pmatrix} fib(n) \\ fib(n-1) \end{pmatrix}
$$

我们希望乘上一个矩阵 \(M\)，得到下一对：
$$
\begin{pmatrix} fib(n+1) \\ fib(n) \end{pmatrix}
$$

#### 2. 拆解方程组

我们要解出矩阵
$
M = \begin{pmatrix} a & b \\ c & d \end{pmatrix}
$
使得：
$$
\begin{pmatrix} a & b \\ c & d \end{pmatrix} \times \begin{pmatrix} fib(n) \\ fib(n-1) \end{pmatrix} = \begin{pmatrix} fib(n+1) \\ fib(n) \end{pmatrix}
$$

按照矩阵乘法展开，得到两个等式：

**第一行（对应 \(fib(n+1)\)）：**
$$
a \cdot fib(n) + b \cdot fib(n-1) = fib(n+1)
$$

因为 \(fib(n+1) = fib(n) + fib(n-1)\)，所以对比系数可以得到：
$$
a = 1, \quad b = 1
$$

**第二行（对应 \(fib(n)\)）：**
$$
c \cdot fib(n) + d \cdot fib(n-1) = fib(n)
$$

所以它的系数必须是 
$
(1 \times fib(n) + 0 \times fib(n-1))
$
因此：
$$
c = 1, \quad d = 0
$$

#### 3. 得出矩阵结论

把求出来的 \(a, b, c, d\) 代进去，得到：
$$
M = \begin{pmatrix} 1 & 1 \\ 1 & 0 \end{pmatrix}
$$

**所以：**
$$
\begin{pmatrix} 1 & 1 \\ 1 & 0 \end{pmatrix} \times \begin{pmatrix} fib(n) \\ fib(n-1) \end{pmatrix} = \begin{pmatrix} fib(n+1) \\ fib(n) \end{pmatrix}
$$

---

### 为什么初始矩阵是 `[1, 0]`？

初始向量
$
\begin{pmatrix} 1 \\ 0 \end{pmatrix}
$
代表的是数列的前两项 \(fib(1)=1\) 和 \(fib(0)=0\)。

我们来验证一下：

* **乘 0 次方（初始状态）：**
  $$
  \begin{pmatrix} 1 \\ 0 \end{pmatrix} \Rightarrow fib(1) = 1, fib(0) = 0
  $$

* **乘 1 次方（右乘矩阵）：**
  $$
  \begin{pmatrix} 1 & 1 \\ 1 & 0 \end{pmatrix} \times \begin{pmatrix} 1 \\ 0 \end{pmatrix} = \begin{pmatrix} 1 \cdot 1 + 1 \cdot 0 \\ 1 \cdot 1 + 0 \cdot 0 \end{pmatrix} = \begin{pmatrix} 1 \\ 1 \end{pmatrix}
  $$
  此时为:
$
\begin{pmatrix} 1 \\ 1 \end{pmatrix}
$
对应 \(fib(2) = 1, fib(1) = 1\)。

* **乘 2 次方：**
  $$
  \begin{pmatrix} 1 & 1 \\ 1 & 0 \end{pmatrix} \times \begin{pmatrix} 1 \\ 1 \end{pmatrix} = \begin{pmatrix} 2 \\ 1 \end{pmatrix}
  $$
  此时为:
$
\begin{pmatrix} 2 \\ 1 \end{pmatrix}
$
对应 \(fib(3) = 2, fib(2) = 1\)。

* **乘 3 次方：**
  $$
  \begin{pmatrix} 1 & 1 \\ 1 & 0 \end{pmatrix} \times \begin{pmatrix} 2 \\ 1 \end{pmatrix} = \begin{pmatrix} 3 \\ 2 \end{pmatrix}
  $$
  此时为:
$
\begin{pmatrix} 3 \\ 2 \end{pmatrix}
$
对应 \(fib(4) = 3, fib(3) = 2\)。

---

### 最终结论（快速幂的由来）

所以，如果我们想求 \(fib(n)\)，只需要让初始向量乘上矩阵 \(M\) 的 \((n-1)\) 次方即可：

$$
\begin{pmatrix} 1 & 1 \\ 1 & 0 \end{pmatrix}^{n-1} \times \begin{pmatrix} 1 \\ 0 \end{pmatrix} = \begin{pmatrix} fib(n) \\ fib(n-1) \end{pmatrix}
$$

而在这个结果矩阵里，**左上角的元素**（\(res[0][0]\)）就是 \(fib(n)\)！

因为矩阵乘法满足结合律，所以我们可以用 **矩阵快速幂** 在 **(O(log n))** 的时间复杂度内求出这个值，这也是为什么当你把矩阵写成 
$
\begin{pmatrix} 1 & 1 \\ 1 & 0 \end{pmatrix}
$
并用 `matrix_pow(matrix, n-1, 2)` 时，返回的 `res[0][0]` 就是正确答案的原因。

### 引用:力扣509题
题目链接:https://leetcode.cn/problems/fibonacci-number/description/

代码如下:
```cpp
class Solution {
public:
typedef long long ll; // 定义长整型别名，防溢出
// 矩阵乘法核心函数
vector<vector<ll>> mul(const vector<vector<ll>> &a, const vector<vector<ll>> &b, int n){
    // 初始化一个 n*n 的全 0 结果矩阵
    vector<vector<ll>> res(n, vector<ll>(n, 0));
    // 遍历左矩阵的行 i
    for (int i = 0; i < n; i++){
        // 遍历左矩阵的列 k（同时也相当于右矩阵的行 k）
        for (int k = 0; k < n; k++){
            if (a[i][k] == 0) continue; // 剪枝优化：若左矩阵该项为0，直接跳过后续乘法
            // 遍历右矩阵的列 j
            for (int j = 0; j < n; j++){
                // 对应位置相乘再累加到结果矩阵中
                // 核心公式：C[i][j] = Σ(A[i][k] * B[k][j])
                res[i][j] = (res[i][j] + a[i][k] * b[k][j]);
            }
        }
    }
    return res; // 返回矩阵乘法结果
}
// 矩阵快速幂核心函数
vector<vector<ll>> matrix_pow(vector<vector<ll>> matrix, ll power, int n) {
    // 初始化结果矩阵为单位矩阵（对角线为1，其余为0）
    vector<vector<ll>> res(n, vector<ll>(n, 0));
    for (int i = 0; i < n; i++) res[i][i] = 1;
    
    // 快速幂（二进制拆分思想）
    while (power > 0){
        if (power & 1) res = mul(res, matrix, n); // 若当前二进制位为1，结果矩阵乘以底数矩阵
        matrix = mul(matrix, matrix, n);           // 底数矩阵自乘（平方）
        power >>= 1;                               // 指数右移一位
    }
    return res; // 返回矩阵快速幂结果
}
// 求斐波那契数列第 n 项
int fib(int n) {
    if(n<=1) return n; // 特判：fib(0)=0, fib(1)=1
    
    // 构造斐波那契递推矩阵 M
    vector<vector<ll>> matrix={
        {1,1},
        {1,0}
    };
    // 求 M 的 (n-1) 次方
    // 初始向量是 [fib(1), fib(0)] = [1, 0]
    // M^(n-1) 的左上角元素即为 fib(n)
    auto res = matrix_pow(matrix, n-1, 2);
    return res[0][0]; // 返回左上角元素
    }
};
```
* 其实矩阵快速幂不是只能解决**斐波拉契** 数列的问题，它还可以解决形如:

`f(n)=a*f(n-1)+b*f(n-2)+c*f(n-3)......`递推式的问题，那么我们要变化的无非是**初始矩阵**和**每次要乘上的矩阵**就行了，那么要怎么改呢，请看一下:

### 矩阵快速幂解决任意线性递推式

其实矩阵快速幂不是只能解决斐波拉契数列的问题，它还可以解决形如：

`f(n) = a*f(n-1) + b*f(n-2) + c*f(n-3)......` 递推式的问题。

那么我们要变化的无非是**初始矩阵**和**每次要乘上的矩阵**就行了。

#### 1. 理论基础：状态转移矩阵

假设有一个 **k 阶**的线性递推式：
$$
f(n) = c_1 f(n-1) + c_2 f(n-2) + \dots + c_k f(n-k)
$$
其中 
$
(c_1, c_2, \dots, c_k)
$
为已知常数。

我们构造一个 **k x 1 的列向量（状态向量）** 来表示当前连续的 k 项：
$$
S_n = \begin{pmatrix} f(n) \\ f(n-1) \\ f(n-2) \\ \vdots \\ f(n-k+1) \end{pmatrix}
$$

目标是找到一个 **k x k 的转移矩阵 M**，使得：
$$
S_{n+1} = M \times S_n
$$

---

#### 2. 转移矩阵 M 的构造模板

根据斐波那契的经验，转移矩阵的**第一行**存放递推式的系数，**对角线下方**全放 1，其余位置全放 0。

**对于 k 阶递推式，通用的矩阵模板如下：**
$$
M = \begin{pmatrix}
c_1 & c_2 & c_3 & \cdots & c_{k-1} & c_k \\
1 & 0 & 0 & \cdots & 0 & 0 \\
0 & 1 & 0 & \cdots & 0 & 0 \\
\vdots & \vdots & \vdots & \ddots & \vdots & \vdots \\
0 & 0 & 0 & \cdots & 1 & 0
\end{pmatrix}
$$

---

#### 3. 验证：为什么这样构造是对的？

我们计算 
$
M \times S_n\
$
的第一行，它等于：
$$
c_1 f(n) + c_2 f(n-1) + \dots + c_k f(n-k+1) = f(n+1)
$$
这完美符合递推公式！

而接下来的每一行（例如第 2 行）：
$$
1 \times f(n) + 0 \times f(n-1) + \dots + 0 = f(n)
$$
也就是让状态向量里的元素依次“向下平移”一位。这正是我们想要的“往前推一项”的效果。

---

#### 4. 具体例子（看你怎么套用）

##### 例 1：斐波那契数列 f(n) = f(n-1) + f(n-2)

这里 k=2, c1=1, c2=1。
$$
M = \begin{pmatrix} 1 & 1 \\ 1 & 0 \end{pmatrix}
$$
状态向量为 
$
\begin{pmatrix} f(n) \\ f(n-1) \end{pmatrix}
$
，初始向量为 
$
\begin{pmatrix} f(1) \\ f(0) \end{pmatrix} = \begin{pmatrix} 1 \\ 0 \end{pmatrix}
$

##### 例 2：三阶递推 f(n) = 3f(n-1) + 2f(n-2) + 5f(n-3)

这里 k=3, c1=3, c2=2, c3=5。
$$
M = \begin{pmatrix} 3 & 2 & 5 \\ 1 & 0 & 0 \\ 0 & 1 & 0 \end{pmatrix}
$$
状态向量为 
$
\begin{pmatrix} f(n) \\ f(n-1) \\ f(n-2) \end{pmatrix}
$
要算 f(N)，初始向量必须给出前 3 项 f(2), f(1), f(0)（或者题目给的起始项），然后求 M^(N-2)（具体指数取决于初始向量的起始位置），最后取结果向量的第一行第一列。

##### 例 3：带有常数项的递推 f(n) = 2f(n-1) + 3

虽然多了一个常数项，但我们通过“升维”把它变成 2 阶线性递推。令 g(n) = 1，那么：
$$
\begin{pmatrix} f(n+1) \\ 1 \end{pmatrix} = \begin{pmatrix} 2 & 3 \\ 0 & 1 \end{pmatrix} \times \begin{pmatrix} f(n) \\ 1 \end{pmatrix}
$$
这样就完美解决常数项的问题了！

---

#### 5. 核心总结

| 递推式阶数 k | 矩阵大小 | 矩阵第一行 | 矩阵其余部分 |
| :--- | :--- | :--- | :--- |
| 1 阶 | 1 x 1 | [c1] | 无 |
| 2 阶 | 2 x 2 | [c1, c2] | 第二行 [1, 0] |
| 3 阶 | 3 x 3 | [c1, c2, c3] | 第二行 [1, 0, 0]；第三行 [0, 1, 0] |
| k 阶 | k x k | [c1, c2, ..., ck] | 次对角线下方全为 1，其余全为 0 |



  