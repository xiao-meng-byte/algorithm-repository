# 优先队列和单调队列的浅浅学习
事实上我在一开始学它们的时候，觉得是两种不同的队列，但实际上单调队列是一种算法，用双端队列来实现，不过我们先看一段有点意思的代码：

```java
import java.io.*;
import java.util.*;

public class Main {
    public static int getMax(int[] arr) {
        return fun(arr, 0, arr.length - 1);
    }
    public static int fun(int[] arr, int left, int right) {
        if (left == right) {
            return arr[left];
        }
        int mid = left + (right - left)>>1;
        int leftMax = fun(arr, left, mid);
        int rightMax = fun(arr, mid + 1, right);
        return Math.max(leftMax, rightMax);
    }
    public static void main(String[] args) throws IOException {
        // 自动生成 10 万个随机数进行测试
        int n = 100000;
        int[] arr = new int[n];
        for (int i = 0; i < n; i++) {
            arr[i] = (int) (Math.random() * 1000000); // 生成 0 到 100万的随机数
        }
        long start1 = System.nanoTime();
        int Max1 = getMax(arr);
        System.out.println(Max1);
        long end1 = System.nanoTime();

        long start2 = System.nanoTime();
        int Max2 = Integer.MIN_VALUE;
        for (int i = 0; i < n; i++) {
            if (arr[i] > Max2) {
                Max2 = arr[i];
            }
        }
        System.out.println(Max2);
        long end2 = System.nanoTime();
        long start3 = System.nanoTime();
        PriorityQueue<Integer> pq = new PriorityQueue<>((o1, o2) -> o2 - o1);
        for (int i = 0; i < n; i++) {
            pq.offer(arr[i]);
        }
        int Max3 = pq.poll();
        System.out.println(Max3);
        long end3 = System.nanoTime();
        // 因为方法四排序会改变原数组 arr，所以这里我们得用 arr 的拷贝，或者把方法四放最后
        int[] arrCopy = arr.clone();
        long start4 = System.nanoTime();
        Arrays.sort(arrCopy);
        int Max4 = arrCopy[arrCopy.length - 1];
        System.out.println(Max4);
        long end4 = System.nanoTime();
        System.out.println("方法一(分治)用时: " + (end1 - start1) / 1000 + " 微秒");
        System.out.println("方法二(遍历)用时: " + (end2 - start2) / 1000 + " 微秒");
        System.out.println("方法三(优先队列)用时: " + (end3 - start3) / 1000 + " 微秒");
        System.out.println("方法四(排序)用时: " + (end4 - start4) / 1000 + " 微秒");
    }
}
```

在上面我用了四种方法来实现求数组的最大值，分别为:

**①分治递归**

**②循环更新**

**③优先队列(大根堆)**

**④排序取值**

随机生出10万个数字，用System.nanoTime（**精确到纳秒**）来计算运行用时，结果如下：

事实上，每个人的计算及配置不一样，运行Java时的虚拟机也可能不同，如果真的要评判它们的运行谁更快，其实客观上来说是时间复杂度，如下：

| 方法 | 核心实现 | 时间复杂度 | 空间复杂度 | 10 万级数据实际表现 | 适用场景 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 方法一：分治递归 | 二分递归比较左右区间最大值 | O(n) | O(logn)（递归栈开销） | 较快，受递归函数调用开销影响 | 分治算法学习、理解递归思想 |
| 方法二：直接遍历 | 单次循环逐个比较 | O(n) | O(1) | 最快，无额外开销 | 静态数组查找最值（最优通用解法） |
| 方法三：大根堆 | 全部入堆后取堆顶 | O(nlogn) | O(n)（堆存储） | 最慢，堆调整、装箱开销大 | 动态数据流、滑动窗口、反悔贪心 |
| 方法四：排序取末尾 | 双轴快排后取最后元素 | O(nlogn) | O(logn)（排序栈开销） | 较慢，内置排序高度优化 | 需同时获取有序序列 + 最值的场景 |

不过我在这里不是想比较它们谁快，在解题的时候我觉得多一种算法就多一种选择，下面是我学习优先队列的总结：

优先队列(Java---PriorityQueue是默认小根堆，C++---priority_queue是默认的大根堆），最多的写法是手写完全二叉树，代码如下：

优先队列的分析:
```angular2html
// 大根堆二叉树的规则：
// ① 插入的时候，插入的元素应该放在堆的末尾，然后向上调整，使堆的规则满足。
// 就是从左到右顺序，从上到下顺序，把大的值放在堆的顶部。

// 插入[1, 2, 5, 3, 4]

【① 插入1的时候】
1

【② 插入2的时候】
  1            ==>          2
 / \                       / \
2                         1

(发现 2 比 1 大，所以交换位置)

【③ 继续插入5】
  2                         5
 / \        ==>            / \
1   5                     1   2

(发现 5 比 2 大，所以交换位置)

【④ 继续插入3】
    5                          5
   / \                        / \
  1   2         ==>          3   2
 /                          /
3                          1

(发现 3 比 1 大，所以交换位置)

【⑤ 继续插入4】
    5                          5
   / \                        / \
  3   2         ==>          4   2
 / \                        / \
1   4                      1   3

(发现 4 比 3 大，所以交换位置，此时大根堆构建完成)

============================================================

// ② 删除的时候，删除的是堆顶元素，然后向下调整，使堆的规则满足。
// 对插入的内容 [5, 4, 2, 3, 1] 进行取出并删除。

【① 先取5，交换堆顶和末尾】
    3                         3
   / \                       / \
  4   2        ==>          4   2
 / \                       /
1   5                     1

(取出 5 后)

【② 发现 4 比 3 大，向下交换位置】
   3                          4
  / \                        / \
 4   2         ==>          3   2
/                          /
1                          1

【③ 取4，交换堆顶和末尾】
    1                          1
   / \                        / \
  3   2         ==>          3   2
 /                          /
4                          4

(删去 4 后)

【④ 发现 3 比 1 大，向下交换位置】
   3                          3
  / \                        / \
 1   2         ==>          1   2

【⑤ 取3，交换堆顶和末尾】
   2                          2
  / \                        /
 1   3                      1

(删去 3 后)

【⑥ 再交换再取出 2，最后取出 1】
1
```

```java
import java.util.*;
import java.io.*;

class PriorityQueue {
    static PrintWriter out = new PrintWriter(System.out);
    // 定义一个堆数组
    private int[] heap;
    // 堆的容量
    private int size;

    public PriorityQueue(int capacity) {
        // 初始化堆数字的容量
        heap = new int[capacity];
        // 堆的有效长度初始化为0
        size = 0;
    }

    // 插入 → 向上调整
    public void swap(int[] heap, int index, int parent) { // 交换元素,用异或来实现
        heap[index] = heap[index] ^ heap[parent];
        heap[parent] = heap[index] ^ heap[parent];
        heap[index] = heap[index] ^ heap[parent];
    }

    public void insert(int value) {
        // 判断堆数组是否已满
        if (size >= heap.length) {
            return;
        }
        // 将新元素放在堆数组末尾
        heap[size] = value;
        // 向上调整,长度加1
        size++;
        // 定义索引等于长度-1，父节点索引为(index-1)/2
        int index = size - 1;
        while (index > 0) {
            int parent = (index - 1) / 2;
            if (heap[parent] < heap[index]) { // 小根堆的话，判断条件变为>
                swap(heap, index, parent);
                index = parent;
            } else {
                // 不交换，跳出循环
                break;
            }
        }
    }

    public int poll() {
        if (isEmpty()) { // 判断是否为空
            throw new RuntimeException("队列为空!");
        }
        int result = heap[0]; // 堆顶是最大值
        heap[0] = heap[size - 1]; // 最后一个元素放到堆顶
        size--; // 长度减1
        int index = 0; // 记录父节点，就是从顶节点开始
        while (true) {
            // 左子节点索引
            int left = index * 2 + 1;
            // 右子节点索引
            int right = index * 2 + 2;
            int maxIdx = index;
            // 左子节点存在且左子节点值更大，更新最大下标
            if (left < size && heap[left] > heap[maxIdx]) // 小根堆的话，判断条件变为<
                maxIdx = left;
            // 右子节点存在且右子节点值更大，更新最大下标
            if (right < size && heap[right] > heap[maxIdx]) // 小根堆的话，判断条件变为<
                maxIdx = right;
            // 最大值还是自己，无需调整，退出循环
            if (maxIdx == index) break;
            // 和最大的孩子交换
            swap(heap, index, maxIdx);
            // 下标移动到交换后的位置，继续向下调整
            index = maxIdx;
        }
        return result;
    }

    public boolean isEmpty() { // 判断是否为空
        return size == 0;
    }

    public int peek() { // 返回堆顶元素,但不删除
        return heap[0];
    }

    public int size() { // 返回堆中元素个数
        return size;
    }

    public void print() {
        for (int i = 0; i < size; i++) {
            out.print(heap[i] + " ");
        }
        out.println();
    }

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;
        int n = Integer.parseInt(br.readLine().trim());
        PriorityQueue pq = new PriorityQueue(n);
        st = new StringTokenizer(br.readLine());
        for (int i = 0; i < n; i++) {
            int x = Integer.parseInt(st.nextToken());
            pq.insert(x);
        }
        while (!pq.isEmpty()) {
            out.print(pq.poll() + " ");
        }
        out.println();
        out.flush();
        br.close();
        out.close();
    }
}
```

基本的原理就是完全二叉树，大概就是以下总结：

不过其实大多数时候我们调库就行：

| 功能 | Java PriorityQueue | C++ priority_queue |
| :--- | :--- | :--- |
| 默认结构 | 小根堆（堆顶最小） | 大根堆（堆顶最大） |
| 大根堆 | `new PriorityQueue<>((a,b)->b-a)` | `priority_queue<int> pq;` |
| 小根堆 | `new PriorityQueue<>()` | `priority_queue<int, vector<int>, greater<int>> pq;` |
| 添加元素 | `pq.offer(x)` | `pq.push(x)` |
| 查看堆顶 | `pq.peek()` | `pq.top()` |
| 删除堆顶 | `pq.poll()` | `pq.pop()` |
| 获取大小 | `pq.size()` | `pq.size()` |
| 判空 | `pq.isEmpty()` | `pq.empty()` |
| 清空 | `pq.clear()` | 循环 pop() 或重新定义 |
| 底层结构 | 二叉堆（数组） | 二叉堆（默认 vector） |

引用：牛客noob107 结构体优先队列：
题目链接: https://www.nowcoder.com/practice/e3d68ce7184e4658b42740edd4308d51?tpId=383&tqId=11211859&channelPut=tracker1

描述：
期末考试结束了，老师开始忙着给每个同学登记成绩。这次考试共有语文、数学和外语三门科目。登记过程中有如下三种操作：
- 操作 `1 x y z`：登记一位同学的成绩，语文 x 分、数学 y 分、外语 z 分；
- 操作 `2`：输出当前成绩最好的同学的三门成绩；
- 操作 `3`：删除成绩最好的同学的记录（若有并列，则只删除一人）。

“成绩最好” 的判定规则为：首先比较总成绩 x+y+z，总成绩高者更好；若相同，则比较语文成绩 x；若仍相同，则比较数学成绩 y；再相同时比较外语成绩 z。

输入描述：
第一行输入一个整数 n（1 <= n <= 10^6），表示操作次数。
接下来 n 行，每行是一次操作：
- 若操作类型为 `1`，则形式为 `1 x y z`，其中 1 <= x,y,z < 100；
- 若操作类型为 `2` 或 `3`，则形式为单个整数 `2` 或 `3`。

输出描述：
对于每次操作 `2`，在一行中输出 `x y z`，表示该次查询时成绩最好的同学的语文、数学和外语成绩。

代码如下：

```cpp
#include<bits/stdc++.h>
using namespace std;

struct node { // 定义一个结构体
    int chinese, math, english, sum;
};

bool operator<(node a, node b) { // 实际上是一个比较器的仿函数
    if (a.sum != b.sum) return a.sum < b.sum;
    else if (a.chinese != b.chinese) return a.chinese < b.chinese;
    else if (a.math != b.math) return a.math < b.math;
    else return a.english < b.english;
}

priority_queue<node> s;

void insertValue(int chinese, int math, int english) {
    node t; // 定义一个结构体变量t
    t.chinese = chinese;
    t.math = math;
    t.english = english;
    t.sum = chinese + math + english;
    s.push(t);
}

void deleteValue() {
    s.pop();
}

node getTop() {
    return s.top();
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int q, op;
    int x, y, z;
    cin >> q;
    while (q--) {
        cin >> op;
        if (op == 1) {
            cin >> x >> y >> z;
            insertValue(x, y, z);
        }
        if (op == 2) {
            node tmp = getTop();
            cout << tmp.chinese << " " << tmp.math << " " << tmp.english << endl;
        }
        if (op == 3) {
            deleteValue();
        }
    }
    return 0;
}
```

### 二. 单调队列

C++、Java的双端队列基本用法如下：

| 功能 | C++ (deque) | Java (Deque/ArrayDeque) |
| :--- | :--- | :--- |
| 头文件 / 导入 | `#include <deque>` | `import java.util.Deque; import java.util.ArrayDeque;` |
| 初始化 | `deque<int> q;` | `Deque<Integer> q = new ArrayDeque<>();` |
| 队尾插入 | `q.push_back(x);` | `q.addLast(x);` |
| 队头插入 | `q.push_front(x);` | `q.addFirst(x);` |
| 取队头元素 | `q.front();` | `q.peekFirst();` |
| 取队尾元素 | `q.back();` | `q.peekLast();` |
| 删除队头 | `q.pop_front();` | `q.pollFirst();` |
| 删除队尾 | `q.pop_back();` | `q.pollLast();` |
| 判断非空 | `!q.empty()` | `!q.isEmpty()` |
| 获取大小 | `q.size()` | `q.size()` |

以上其实只是容器/集合使用，就是加深对基础知识的理解。

实际上我接触它的时候是源于这个题：

引用洛谷P1886 【模板】单调队列 / 滑动窗口:
题目链接:https://www.luogu.com.cn/problem/P1886

题目描述：
有一个长为 n 的序列 a，以及一个大小为 k 的窗口。现在这个窗口从左边开始向右滑动，每次滑动一个单位，求出每次滑动后窗口中的最小值和最大值。

例如，对于序列 [1, 3, -1, -3, 5, 3, 6, 7] 以及 k = 3，有如下过程：

| 窗口位置 | 最小值 | 最大值 |
| :--- | :--- | :--- |
| [1 3 -1] -3 5 3 6 7 | -1 | 3 |
| 1 [3 -1 -3] 5 3 6 7 | -3 | 3 |
| 1 3 [-1 -3 5] 3 6 7 | -3 | 5 |
| 1 3 -1 [-3 5 3] 6 7 | -3 | 5 |
| 1 3 -1 -3 [5 3 6] 7 | 3 | 6 |
| 1 3 -1 -3 5 [3 6 7] | 3 | 7 |
输入格式：
输入一共有两行，第一行有两个正整数 n, k；
第二行有 n 个整数，表示序列 a。

输出格式：
输出共两行，第一行为每次窗口滑动的最小值；
第二行为每次窗口滑动的最大值。

输入输出样例 #1：
输入 #1
```text
8 3
1 3 -1 -3 5 3 6 7
```

输出 #1
```text
-1 -3 -3 -3 3 3
3 3 5 5 6 7
```

说明/提示：
【数据范围】

对于 50% 的数据，1 <= n <= 10^5；

对于 100% 的数据，1 <= n <= 10^6，a_i ∈ [-2^31, 2^31)。

因为很久之前我学过滑动窗口，现在刷题的时候就写到了这个题，这个题怎么写呢，本质上之前没学这个单调队列的时候，我就觉得可以暴力解，大概就是这样的：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, k;
    cin >> n >> k;
    vector<int> arr(n);
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }
    for (int i = 0; i <= n - k; i++) {
        int Min = INT_MAX;
        for (int j = i; j < i + k; j++) {
            Min = min(arr[j], Min);
        }
        cout << Min << " ";
    }
    cout << endl;
    for (int i = 0; i <= n - k; i++) {
        int Max = INT_MIN;
        for (int j = i; j < i + k; j++) {
            Max = max(arr[j], Max);
        }
        cout << Max << " ";
    }
    return 0;
}
```

可想而知就超时了，所以我们在维护窗口的同时，我们可以思考在窗口遍历的时候我发现进数组的元素比前面的大（或者小）我可不可以就不用看前面的元素，就是删掉它们，属于一个动态的过程，到窗口末尾的时候就停止呢，这样我们的效率就快了不少，本质上双端队列就是有首尾都可出进的功能，所以就可以用他来思考，不一定用它，其实手动模拟也行，所以用C语言来模拟：

```c
#include <stdio.h>
#define MAXN 1000005
int a[MAXN];  // 存储原始数组
int q[MAXN];  // 这里用数组模拟队列，队列里存的是【下标】，不是数组的值
int front, rear;  // 定义队列头、尾指针
// 函数：求滑动窗口最小值（单调递增队列）
void slidingWindowMin(int n, int k) {
    front = 0, rear = -1;  // 初始化队列：rear=-1就是说空队列
    for (int i = 0; i < n; i++) {
        // 首先：维护单调递增
        // 队列不为空且当前数 <= 队尾下标对应的数
        // 说明队尾那个数不可能成为最小值了，所以直接删掉
        while (front <= rear && a[i] <= a[q[rear]])
            rear--;  // 队尾出队
        // 把当前下标加入队尾
        q[++rear] = i;
        // 然后：删掉窗口外的过期元素
        // 如果队头下标已经滑出窗口左边界，就删掉
        while (q[front] <= i - k)
            front++;
        // 最后窗口形成，输出队头（最小值）
        if (i >= k - 1)
            printf("%d ", a[q[front]]);
    }
}
// 函数：求滑动窗口最大值（单调递减队列）
void slidingWindowMax(int n, int k) {
    front = 0, rear = -1;  // 重置队列
    for (int i = 0; i < n; i++) {
        // 首先：维护单调递减
        // 当前数 >= 队尾数 → 队尾没用了，删掉
        while (front <= rear && a[i] >= a[q[rear]])
            rear--;
        // 当前下标入队
        q[++rear] = i;
        // 然后：删掉窗口外过期元素
        while (q[front] <= i - k)
            front++;
        // 最后：输出队头（最大值）
        if (i >= k - 1)
            printf("%d ", a[q[front]]);
    }
}
int main() {
    int n, k;
    scanf("%d%d", &n, &k);
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }
    slidingWindowMin(n, k);
    printf("\n");
    slidingWindowMax(n, k);
    return 0;
}
```

## 总结一下：

在C++、Java中有很多的工具集合啊，容器啊这一类的，其实对我来说，我最欠缺的是理解一些容器/集合的底层代码，只是知其用法，大多对原理不是那么的熟，因为我还没有系统性的学习数据结构，这个学期我们学校的主课是Java编程设计，C++容器还是自学的，对于今天总结的这些，还是比较浅的，没有更深入了解它们的应用，比如说洛谷：优先队列+贪心，不太会写，所以我还是初学这一内容的，到了暑假才有时间系统性学习数据结构，所以学一点总结一点为好。
