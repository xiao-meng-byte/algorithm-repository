# 基础查集的学习
## 并查集的使用场景
* 一开始每个元素都拥有自己的集合，在自己的集合里只有这个元素自己
* **int find(i)** 方法: 查找i所在的集合的代表元素，代表元素来代表i所在的集合
* **boolean isSameSet(a,b)** : 判断`a`和`b`在不在同一个集合里
* **void union(a,b)**: `a`所在的集合的所有元素和`b`所在集合的所有元素合并为一个集合
* 各种操作的单次调用的均摊时间复杂度为**o(1)**
## 并查集的原理
```bash
首先我们有{a},{b},{c},{d},{e},{f}这6个集合
一开始每个集合自己都是自己的父亲:
a -> a      b -> b      c -> c      d -> d      e -> e      f -> f
(圈)        (圈)        (圈)        (圈)        (圈)        (圈)
现在合并集合{a},{b}:
a -> b 让a集合指向b 
同样的我们合并{e},{f}:
e -> f
现在合并前面已经合并的两个集合就只要让b的指针指向f就可以了，代价较小
而int find(i)是寻找他的根节点，就是往上找指针，一直往上到不能再往上就找到i的根节点
而boolean isSameSet(a,b) 是看他们的根节点是否相同，如果相同就说明是在同一个集合里
而对于void isSameSet 就可以好好分析一下了:
father 数组:元素(初始的集合)           [0,1,2,3,4,5]
                                    ↓ ↓ ↓ ↓ ↓ ↓
          根节点(一开始是自己指向自己): [0,1,2,3,4,5]
size 数组: 每个集合的数值量(一开始都是1) [1,1,1,1,1,1]
首先我们演示一下0和1合并的过程:
更新的时候，首先让0 -> 1根节点就变为: [1,1,2,3,4,5]
size 的大小就变为: [1,2,1,1,1]
而在后续的合并过程中我们一般都是 **小挂大** ，即包含元素少的集合合并的时候挂到多的集合下面，而我们还有一种优化: 扁平化
比如说:我们现在有一个这样的集合:
0 -> 1 -> 3 -> 2 -> 4 -> 6 这样的我们发现我们递归寻找0的根节点的时候就发现路劲过长，而我们如果有更多的节点挂到上面去的话:
就会减低效率，这样我们就可以扁平化:让0直接指向6，这样我们的find(i)方法的效率就大大提高了。
```
## 模板代码和例题
代码如下:
```cpp
#include <bits/stdc++.h>
using namespace std;
class DSU {
private:
    static const int MAXN = 10000;
    int father[MAXN];
    int Mysize[MAXN];
public:
    // 构造函数：初始化并查集
    DSU(int n) {
        init(n);
    }
    void init(int n) {
        for (int i = 1; i <= n; i++) {
            father[i] = i;
            Mysize[i] = 1;
        }
    }
    // 查找根节点（带路径压缩）
    int find(int x) {
        if (x != father[x]) {
            father[x] = find(father[x]);
        }
        return father[x];
    }
    // 合并集合（按秩合并，小挂大）
    void unite(int x, int y) {
        int px = find(x);
        int py = find(y);
        if (px != py) {
            if (Mysize[px] >= Mysize[py]) {
                Mysize[px] += Mysize[py];
                father[py] = px; 
            } else {
                Mysize[py] += Mysize[px];
                father[px] = py;
            }
        }
    }
    // 判断是否在同一集合
    bool is_same_set(int x, int y) {
        return find(x) == find(y);
    }
};
```
例题:引用洛谷p1551:亲戚

题目链接:https://www.luogu.com.cn/problem/P1551

代码如下:
```cpp
#include <bits/stdc++.h>
using namespace std;
const int N=100010;
int parent[N];//表示它的父节点
int Mysize[N];//表示他的长度，即根结点以下挂的长度
int Mystack[N];//提供一个空间去临时存他的状态
void init(int n) {
    for (int i=1;i<=n;i++) {
        parent[i]=i;//初始化，每个都是自己的父亲
        Mysize[i]=1;//初始化，每个集合的长度都是1
    }
}
//这是非递归写法也可以写成
/*
 int find(int n){//递归写法，也具备压缩性
      if(parent[n]!=n){
          parent[n]=find(parent[n]);
    }
    return parent[n];
 }
 */
int find(int n) {//find方法是去找他的根节点，在这里实现了压缩功能
    int size=0;
    while (n!=parent[n]) {
        Mystack[size++]=n;//压入栈中,收入不是根节点的节点
        n=parent[n];//找父节点,就是让n向上指，直到找到跟节点
    }
    while ( size>0) {
        parent[Mystack[--size]]=n;//弹出栈，将所有节点都指向根节点
    }
    return n;//返回根节点
}
void Union(int x,int y) {//合并两个集合
    //首先去找他的根节点
    int fx=find(x);
    int fy=find(y);
    if (fx!=fy) {//在不同的情况下，合并,我们这里是大吞小
        if (Mysize[fx]>=Mysize[fy]) {//大的合并到小的
            Mysize[fx]+=Mysize[fy];
            parent[fy]=fx;//让原来小容量的结点指向大的根节点
        }else {
            Mysize[fy]+=Mysize[fx];;
            parent[fx]=fy;
        }
    }
}
//递归写法的Union方法:这个写法下就没有小挂大的设定了
//void Union(int x,int y){
//  father[find(x)]=find(y);
//}
bool is_same_set(int x,int y) {
    return find(x)==find(y);//判断是否同一个集合
}
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n,m,p;//n个人，m个关系，p个查询
    cin>>n>>m>>p;
    init(n);//初始化并查集
    for(int i=0;i<m;i++){
        int a,b;
        cin>>a>>b;
        Union(a,b);//合并所有集合
    }
    while(p-->0){
        int a,b;
        cin>>a>>b;
        if(is_same_set(a,b)){//判断是否同一个集合，就说明具有亲戚关系
            cout<<"Yes"<<endl;
        }else{
            cout<<"No"<<endl;
        }
    }
    return 0;
}
```
## 进阶技巧和题目
### 题目一:力扣情侣牵手
题目链接:https://leetcode.cn/problems/couples-holding-hands/description/
### 题目描述
n 对情侣坐在连续排列的 2n 个座位上，想要牵到对方的手。

人和座位由一个整数数组 row 表示，其中 row[i] 是坐在第 i 个座位上的人的 ID。情侣们按顺序编号，第一对是 (0, 1)，第二对是 (2, 3)，以此类推，最后一对是 (2n - 2, 2n - 1)。

返回 最少交换座位的次数，以便每对情侣可以并肩坐在一起。 每次交换可选择任意两人，让他们站起来交换座位。

### 示例 1:

输入: row = [0,2,1,3]
输出: 1
解释: 只需要交换第二个人（row[1]）和第三个人（row[2]）的位置即可。
### 示例 2:

输入: row = [3,2,0,1]
输出: 0
解释: 无需交换座位，所有的情侣都已经可以手牵手了。


### 提示:

2n == row.length
2 <= n <= 30
n 是偶数
0 <= row[i] < 2n
row 中所有元素均 无重复

### 思考规律
```bash
1. 初步观察与举例
根据题意，我们可以举个例子：[6, 0, 8, 9, 1, 7, 3, 4, 5, 2] 这样的初始座位次序。
在这种情况下，我们可以把 1 和 6 交换，4 和 2 交换，就变为 [1, 0, 8, 9, 6, 7, 3, 2, 5, 4]，只需要交换 2 次，这样就符合题意了。
那么，更多的情况会是啥规律呢？
结论：如果说我们有 k 对混在一起的情侣，最少只需交换 k - 1 次即可排好。
2. 核心思路：情侣编号（并查集/连通分量）
我们可以对每个人进行编号：
编号的规则是 0 和 1 视为第 0 对情侣，2 和 3 视为第 1 对情侣，以此类推。所以 x 的情侣编号就是 x / 2（整除）。
我们对第一个例子进行编号：
原编号：   [6, 0, 8, 9, 1, 7, 3, 4, 5, 2]
情侣编号： [3, 0, 4, 4, 0, 3, 1, 2, 2, 1]
我们把相邻位置（即 0 和 1 位置、2 和 3 位置...）视为一桌。
可以看到，上面这 5 桌的情侣编号组合分别是：{3, 0}、{4, 4}、{0, 3}、{1, 2}、{2, 1}。
把这些组合里的所有元素（即所有的情侣编号）作为节点，如果两个编号出现在同一桌上，我们就用线连起来（即并查集合并）。
{3, 0} 和 {0, 3} 构成了一个连通分量：{3, 0}。
{4, 4} 自身构成了一个连通分量：{4}。
{1, 2} 和 {2, 1} 构成了一个连通分量：{1, 2}。
图论结论：
一共有 3 个连通分量。每个连通分量的大小如果为 k，则需要的交换次数为 k - 1。
所以总交换次数为：(2 - 1) + (1 - 1) + (2 - 1) = 1 + 0 + 1 = 2 次。
3. 再举一例验证
我们另举一个例子：[6, 0, 1, 5, 2, 3, 8, 4, 7, 9]
原编号：   [6, 0, 1, 5, 2, 3, 8, 4, 7, 9]
情侣编号： [3, 0, 0, 2, 1, 1, 4, 2, 3, 4]
相邻成对的分组为：{3, 0}、{0, 2}、{1, 1}、{4, 2}、{3, 4}。
找出其连通分量（并查集合并）：
由 {3, 0} 和 {0, 2} 以及 {4, 2} 和 {3, 4} 构成了一个大环：{3, 0, 2, 4}（共 4 个节点）。
由 {1, 1} 自身构成一个分量：{1}（共 1 个节点）。
总交换次数 = (4 - 1) + (1 - 1) = 3 + 0 = 3 次。
4. 最终结论与算法总结
建图/并查集：遍历数组，每次取相邻的两个位置，把它们的情侣编号用并查集 union 在一起。
统计：数出总的连通分量个数，记为 cnt。
答案：设总共有 n 对情侣，则最少交换次数 = n - cnt。
因为一共有 n 对情侣，也就是有 n 个单独的节点，最终合并成了 cnt 个连通分量，每个分量需要 大小-1 次交换，加总起来就是 n - cnt。
```
### 代码
所以基于以上的思考，代码如下:
```cpp
class Solution {
public:
    // 定义最大节点数，防止数组越界
    const static int N = 1000010;
    static int father[N];
    static int Mysize[N];
    static int sets;  // 记录当前有多少个独立的连通分量（集合）
    // 并查集的初始化
    void init(int n) {
        // 遍历前 n 个节点
        for (int i = 0; i < n; i++) { 
            father[i] = i;   // 一开始每个节点的父亲都是自己
            Mysize[i] = 1;   // 一开始每个集合的大小都是 1
        }
        sets = n;            // 一开始有 n 个独立的集合
    }
    // 查找：找 x 的根节点（带路径压缩）
    int find(int x) {
        if (father[x] != x) {
            // 递归查找父亲，并在回溯时把沿途的节点直接连到根节点上（扁平化）
            father[x] = find(father[x]);
        }
        return father[x];
    }
    // 合并：将 x 和 y 所在的集合合并（按秩合并）
    void Union(int x, int y) {
        int px = find(x);   // 找 x 的根节点
        int py = find(y);   // 找 y 的根节点
        if (px != py) {     // 如果它们不在同一个集合里
            // 小集合挂到大集合上（Mysize 大的作为新根）
            if (Mysize[px] > Mysize[py]) {
                Mysize[px] += Mysize[py];
                father[py] = px;
            } else {
                Mysize[py] += Mysize[px];
                father[px] = py;
            }
            sets--;         // 合并成功，独立的集合数量减 1
        }
    }
    // 主函数：计算最少交换次数
    int minSwapsCouples(vector<int>& row) {
        // 计算一共有多少对情侣，也就是有多少个节点
        int n = row.size() / 2;
        // 初始化并查集：假设一开始所有情侣编号都“各自为政”
        init(n);
        // 遍历数组，每次跳过 2 个人（看一对位置）
        for (int i = 0; i < row.size(); i += 2) {
            // row[i] / 2 和 row[i+1] / 2 是这一对位置上两个人的“情侣编号”
            // 它们在物理位置上被绑在了一起，所以用 Union 合并起来
            Union(row[i] / 2, row[i + 1] / 2);
        }
        // 总交换次数 = 总情侣对数 - 最终剩余的连通分量数
        // 原理：一个大小为 k 的连通分量，内部的混乱情侣只需要交换 k - 1 次就可以排好。
        // 所以总数等价于 (n - sets)
        return n - sets;
    }
};
// 类外定义静态成员变量（必须的，否则链接报错）
int Solution::father[1000010];
int Solution::Mysize[1000010];
int Solution::sets;
```
### 题目二:6-8 Knowledge(并查集的降维问题)
题目链接:https://www.luogu.com.cn/problem/U694250
### 题目描述
每个人的学识水平化为$0~9$，用$0~9$表示其学识高低程度。众所周知，与新生婴儿讨论微积分是一个非常扯淡的事情，所以，学识不为0的人群才可以参与讨论，
现在给定一个$m ∗ n$的人群，给定每个人的文化程度，每个人仅可和上下左右进行讨论，所以请你求出**学术讨论组的个数**。

### 输入格式:
用空格隔开的整数m，n (m行，n列)矩阵(1≤m，n≤100)。

### 输出格式:
学识组的个数。

### 输入样例:
在这里给出一组输入。例如：
```in
4 10
0674800011
1034560500
0039600651
0000000098
```

### 输出样例:
在这里给出相应的输出。例如：
```out
4
```
### 题目的思考
对该题的思考:
```bash
一、为什么这题可以用并查集？
这道题要求的是“学识讨论组”的个数。本质上，它是在求一个二维网格中，连通的“1”有多少个连通分量（也就是有几块连在一起的有效区域）。
虽然用 DFS（深度优先搜索）或者 BFS（广度优先搜索）可以很直观地解决，但是并查集（DSU）提供了另一种角度：我们将每一个有效的格子看成一个“节点”，将相邻且都为‘1’的格子看成“有联系”，并查集可以把这些有联系的节点归拢到一个集合里。

二、核心难点：二维矩阵的“降维”处理
并查集天生是用来处理一维数据的，它的 father 数组下标都是整数。而这道题是二维矩阵，所以我们必须要做一步转化：降维。
二维降一维公式：一维坐标 = 行号 * 矩阵列数 + 列号。
举个具体的例子：在一个 4行10列 的矩阵中，第 2 行第 3 列的格子坐标是 (2, 3)。
它的一维索引就是：2 * 10 + 3 = 23。
通过这个公式，二维矩阵里的每一个位置，都拥有了一个唯一的、独一无二的整数编号。这样我们就可以放心地把它们存进一维 father 数组里了。

三、详细步骤拆解

第一步：初始化集合（build 方法）
我们要先扫一遍整个矩阵。遇到字符是 '1' 的格子（也就是有效的学识组），我们就做两件事：
1. 给它计算出一维坐标 index。
2. 把它的 father 指向它自己（表示它一开始自成一个集合），并且把全局变量 sets 加 1。
等遍历完所有的格子，sets 的值就是“假设没有任何相邻连接”时的初始孤立节点数量。

第二步：合并相邻节点（union 方法）
题目中说“仅可和上下左右进行讨论”，所以只有在上下左右相邻的 '1' 才能算同一个组。
为了不重复检查、提高效率，当我们再次遍历矩阵的时候，对于每一个有效的格子 (i, j)，我们只需要检查两个方向：
1. 向左看：如果 i 不为 0 且左边的格子 (i, j-1) 也是 '1'，那么它们相邻，我们就调用 union 把这两个格子合并。
2. 向上看：如果 j 不为 0 且上边的格子 (i-1, j) 也是 '1'，那么它们也相邻，我们同样调用 union 合并它们。
（为什么只看左和上？因为遍历是从左到右、从上到下的。如果只看左和上，就能覆盖掉所有的相邻关系，而不会漏掉任何一个，也不会重复计算合并。）

第三步：合并的底层逻辑（find 和 union）
在并查集里，找根节点用 find 方法。我们在 find 里用了“路径压缩”，也就是在递归找根的过程中，直接把子节点的父指针指向最上层的根节点，这样下次再找就非常快了。
在 union 方法中，我们先分别找到两个格子的根节点，如果它们的根节点不一样（说明它们目前不属于同一个集合），我们就把其中一个的根节点指向另一个（father[fx] = fy），并且把全局变量 sets 减 1。
这代表：两个独立的组合并成了一个组，组的个数少了一个。

第四步：输出结果
当遍历完整个矩阵，所有能合并的相邻 '1' 都已经合并完成。此时全局变量 sets 的值，就是最终剩下的连通分量的个数，也就是我们要求的“学术讨论组”的个数。

四、知识技巧总结

1. 降维大法：利用 index = a * cols + b 这个神奇的公式，把二维网格完美地变成了一个一维数组，从而可以套用并查集模板。
2. 相邻检查优化：只检查左和上，避免了 O(4 * n * m) 的冗余检查，只需 O(2 * n * m) 即可完成全部合并，效率极高。
3. 动态维护集合数：不要等最后再去遍历统计，利用 sets 变量，在初始化时加一，在合并时减一，一步到位，实现极其优雅。

```
代码如下:
```java
import java.util.*;
import java.io.*;
public class Main {
    //建立全局变量
    public static int MAXSIZE = 1000;
    public static int[] father = new int[MAXSIZE];
    public static int cols;
    public static int sets;
    public static void build(int n, int m, char[][] grid) {
        cols = m;
        sets = 0;
        for (int a = 0; a < n; a++) {
            for (int b = 0, index; b < m; b++) {
                if (grid[a][b] == '1') {
                    index = index(a, b);
                    father[index] = index;
                    sets++;
                }

            }
        }
    }
    public static int index(int a, int b) {//这是一个降维的操作
        return a * cols + b;
    }
    public static int find(int i) {//递归版的find方法
        if(i!=father[i]){
            father[i]=find(father[i]);
        }
        return father[i];
    }
    public static void union(int a, int b,int c,int d) {//递归版的union方法
        int fx=find(index(a,b));
        int fy=find(index(c,d));
        if(fx!=fy){
            father[fx]=fy;
            sets--;
        }
    }
    public static int numIsGroups(char[][] grid) {
        int n = grid.length;
        int m = grid[0].length;
        build(n, m, grid);//初始化并查集
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                //在这里只要一边判断就行了
                if (grid[i][j] == '1') {
                    if (j > 0 && grid[i][j - 1] == '1') {
                        union(i,j,i,j-1);
                    }
                    if(i>0 && grid[i-1][j] == '1'){
                        union(i,j,i-1,j);
                    }
                }
            }
        }
        return sets;
    }
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StreamTokenizer st = new StreamTokenizer(br);
        PrintWriter out = new PrintWriter(System.out);
        String line = br.readLine();
        String[] parts = line.split(" ");
        int r=Integer.parseInt(parts[0]);
        int c=Integer.parseInt(parts[1]);
        char[][] grid = new char[r][c];
        for (int i = 0; i < r; i++) {
            line = br.readLine();
            for (int j = 0; j < c; j++) {
                grid[i][j]=line.charAt(j);
            }
        }
        for (int i = 0; i < r; i++) {
            for (int j = 0; j < c; j++) {
                if (grid[i][j] != '0') {
                    grid[i][j]='1';
                }
            }
        }
        numIsGroups(grid);
        out.println(sets);
        out.flush();
        out.close();
        br.close();
    }
}
```
### 题目三:6-5 畅通工程(20分)
题目链接: https://pry.jsu.edu.cn/problems/999
### 题目描述
某地区有$N$个村庄，政府希望修建一些公路，使得任意两个村庄之间都可以互相到达（直接或间接）。

已知某些村庄之间可以修建公路，并给出了修建成本。请计算实现村村通公路的最低成本。

### 输入格式
第一行包含两个整数$N$和$M$，分别表示村庄数量和可以修建的公路数量。

接下来M行，每行包含三个整数$A$,$B$,$C$，表示在村庄$A$和村庄$B$之间修建公路的成本为$C$。

### 输出格式
一个整数，表示实现村村通的最低成本。如果无法实现村村通，输出`-1`。

### 输入样例
在这里给出一组输入。例如：

```in
4 5
1 2 1
1 3 4
2 3 2
2 4 7
3 4 3
```

### 输出样例
在这里给出相应的输出。例如：

```out
6
```
思路仔细见分支**MST**(最小生成树)
代码如下:
```java
import java.io.*;
import java.util.*;
public class Main {
    static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
    static PrintWriter out = new PrintWriter(System.out);
    static StreamTokenizer st = new StreamTokenizer(br);
    static int MAXN = 5001;
    static int MAXM = 200001;
    static int n, m;          // n = 节点数（村庄数），m = 边数（可修建的公路数）
    static int[] father = new int[MAXN];           // 并查集：father[i] 表示节点 i 的父节点
    static int[][] edges = new int[MAXM][3];       // 边集：edges[i] = {起点, 终点, 权值}
    static void init() {
        for (int i = 1; i <= n; i++) {
            father[i] = i;   // 每个节点都是自己的根节点
        }
    }
    static int find(int x) {
        if (x != father[x]) {               // 如果 x 不是根节点
            father[x] = find(father[x]);    // 递归查找根节点，并压缩路径
        }
        return father[x];                   // 返回根节点
    }
    static boolean Union(int x, int y) {
        int px = find(x);   // 找到 x 的根节点
        int py = find(y);   // 找到 y 的根节点
        if (px != py) {          // 如果根节点不同，说明不在同一集合
            father[px] = py;     // 将 x 的集合挂到 y 的集合下（简单合并）
            return true;         // 合并成功，这条边可以加入生成树
        }
        return false;            // 根相同，合并失败（加入这条边会形成环）
    }
    public static void main(String[] args) throws IOException {
        // ---- 读取 n 和 m ----
        st.nextToken();          // 读取第一个 token
        n = (int) st.nval;       // 村庄数量
        st.nextToken();          // 读取第二个 token
        m = (int) st.nval;       // 公路数量
        init();
        // ---- 读取 m 条边 ----
        for (int i = 0; i < m; i++) {
            st.nextToken();                    // 读取起点
            edges[i][0] = (int) st.nval;
            st.nextToken();                    // 读取终点
            edges[i][1] = (int) st.nval;
            st.nextToken();                    // 读取权值（修建成本）
            edges[i][2] = (int) st.nval;
        }
        // ---- Kruskal 算法第一步：按权值升序排序 ----
        // 贪心思想：优先选择成本最小的边
        Arrays.sort(edges, 0, m, (a, b) -> a[2] - b[2]);
        // ---- Kruskal 算法第二步：从小到大遍历边 ----
        int ans = 0;    // 记录最小生成树的总权值（最低成本）
        int cnt = 0;    // 记录已选入生成树的边数
        for (int i = 0; i < m; i++) {
            int from = edges[i][0];   // 起点
            int to = edges[i][1];     // 终点
            int w = edges[i][2];      // 权值
            // 如果这条边连接的两个节点不在同一集合（不会形成环）
            if (Union(from, to)) {
                cnt++;            // 选中边数 +1
                ans += w;         // 累加成本
                // 优化：提前退出
                // 最小生成树只需要 n-1 条边，选够了就不用再遍历了
                if (cnt == n - 1) {
                    break;
                }
            }
        }
        // ---- 输出结果 ----
        // 如果选中的边数 == n-1，说明所有节点连通，输出最低成本
        // 否则输出 "orz"（表示图不连通，无法实现村村通）
        out.println(cnt == n - 1 ? ans :-1);
        out.flush();
        br.close();
        out.close();
    }
}
```
## 并查集在项目中的应用
而我们在**学生成绩管理系统**里，首先定义UnionFind类，代码如下：

```java
import java.util.*;
public class UnionFind {
    private HashMap<Student, Student> parent;//用于记录父节点
    private HashMap<Student, Integer> rank;//用于记录树的深度,有利于他的合并，相当与模板里的Mysize[];
    public UnionFind() {
        parent = new HashMap<>();
        rank = new HashMap<>();
    }
    public void addNode(Student s){//添加节点
        if(!parent.containsKey(s)){//如果之前节点不存在，则添加
            parent.put(s, s);//父节点设为自己，相当于模板里的parent[x] = x;
            rank.put(s, 1);//树的深度设为1，相当于模板里的Mysize[x] = 1;
        }
    }
    public Student find(Student s){//查询节点,这里是递归写法
        if(parent.get(s) != s){
            parent.put(s, find(parent.get(s)));
        }
        return parent.get(s);
    }
    public void union(Student s1, Student s2){//合并节点
        Student p1 = find(s1);
        Student p2 = find(s2);
        if(p1 != p2){
            if(rank.get(p1) > rank.get(p2)){
                rank.put(p1, rank.get(p1) + rank.get(p2));
                parent.put(p2, p1);
            } else {
                rank.put(p2, rank.get(p1) + rank.get(p2));
                parent.put(p1, p2);
            }
        }
    }
    public boolean isSameSet(Student s1, Student s2){//判断两个节点是否属于同一个集合
        return find(s1) == find(s2);
    }
}
```

和前面的模板思路相同，就是加入学生类和哈希表，在测试类里面我们就使用它：

```java
private static UnionFind uf = new UnionFind();//先创建对象作为全局变量
```

方法里的应用：

```java
public static void addStudent(Student s) throws Exception {
    synchronized (lock) {//因为我同时用了多线程
        String id = s.getId();
        if (map.containsKey(id)) throw new Exception("学号已存在");
        map.put(id, s);
        uf.addNode(s);
        String comb = getCombination(s);//此方法是用于返会所选的几门科目（3+1+2）
        /*public static String getCombination(Student s) {
            return s.getFirstSubject() + "," + s.getSecondSubject1() + "," + s.getSecondSubject2();
        }*/
        for (Student s1 : map.values()) {//相同组合里的合并到同一个集合里
            if (!s1.getId().equals(id) && getCombination(s1).equals(comb)) {
                uf.union(s, s1);
                break;
            }
        }
    }
}
```

```java
//判断学生是否在同已选科目里
public static boolean isSameGroup(String id1, String id2){
    Student s1 = map.get(id1);
    Student s2 = map.get(id2);
    if(s1 == null || s2 == null){
        return false;
    }
    return uf.isSameSet(s1, s2);
}
```

```java
//统计同组合的人数
public static void statGroupSimple() {
    Map<Student, Integer> groupCount = new HashMap<>();//用哈希表类记录个数
    for (Student s : map.values()) {
        Student root = uf.find(s);
        groupCount.put(root, groupcount.getOrDefault(root, 0) + 1);//这是简写和以下是一样的
        /*
        if(groupCount.containsKey(root)){
            int count=groupCount.get(root);
            groupCount.put(root,count+1);
        }else{
            groupCount.put(root,1);
        }
        */
    }
    System.out.println("选科组合总种类：" + groupCount.size());
    int i = 1;
    for (Student root : groupCount.keySet()) {
        String combo = getCombination(root);
        int people = groupCount.get(root);
        out.println("第" + i + "种 组合：" + combo + "，人数：" + people);//一般都用PrintWriter来输出
        i++;
    }
}
```

```java
//查询同选科的学生，用其中一人的学号
public static void showSameGroup(String id) {
    // 根据学号找学生
    Student target = map.get(id);
    if (target == null) {
        out.println("该学号不存在！");
        return;
    }
    // 找这个学生根节点     
    Student root = uf.find(target);
    out.println("===== 同选科所有同学 =====");
    // 遍历所有学生，根节点一样就是同选科
    for (Student s : map.values()) {
        if (uf.find(s) == root) {
            out.println("学号：" + s.getId()+ "  姓名：" + s.getName());
        }
    }
}
```

```java
//同样的，删除也是一个道理
public static void DeleteSameGroup(String id) {
    // 根据学号找目标学生
    Student target = map.get(id);
    if (target == null) {//注意要判断一下
        System.out.println("学号不存在，无法删除！");
        return;
    }
    // 找到这个选科组的根节点
    Student root = uf.find(target);
    // 先收集要删除的所有学号
    ArrayList<String> deleteList = new ArrayList<>();
    for (Student s : map.values()) {
        if (uf.find(s) == root) {//用find方法去找同一集合的学生
            deleteList.add(s.getId());
        }
    }
    for (String id : deleteList) {
        map.remove(id);//通过学号来删除
    }
}
```