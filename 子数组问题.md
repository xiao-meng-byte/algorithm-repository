# 子数组的总结
## 子数组问题总结

其实对于子数组的问题，之前总结的还不够深，所以我在这里重新总结一下：

| 题型序号 | 题型名称 | 对应原题 | 核心解法 | 时间复杂度 | 平台同源题 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| ① | 普通最大子数组和 | LeetCode 53、洛谷 P1115 | 1. Kadane + 贪心<br>2. 前缀和 + 贪心 | O(n) | 力扣 53；洛谷 P1115 |
| ② | 和约束类子数组 | 全正：LeetCode 209<br>含负：LeetCode 325 | 全正：滑动窗口<br>含负：前缀和 + 哈希 | O(n) | 力扣 209、325 |
| ③ | 长度≤K 最大子数组和 | 洛谷 P1714 | 前缀和 + 单调队列 | O(n) | 洛谷 P1714 |
| ④ | 环形最大子数组和 | LeetCode 918 | 双 Kadane，总和减最小子数组 | O(n) | 力扣 918 |
| ⑤ | 乘积最大子数组 | LeetCode 152 | 双状态 Kadane：同时维护当前最大 / 最小乘积，应对负负得正 | O(n) | 力扣 152 |

### ①. 普通最大子数组和

这个我现在所会的方法就三种：①暴力解法，②前缀和+贪心，③kadane＋贪心，事实上还可以用莫队来解，不过我并不熟悉这种写法，首先前缀和就是把原数组前多少项加起来，那么kadane算法是啥（其实我和我同学一般叫他老登算法哈哈~~~）：

本质上我们再求子数组的和最大的时候，我们遍历数组，我们把每一个数字都看作我们子数组部分的末尾，求他前面部分的加和，如果前面因为有负数让加和小于当前末尾值了，那就将子数组的开头移到这个末尾值来，重新遍历，不断更新最大值就行了，这种思想可以用到最长连续上升子序列里去，就是如果上升被打断了，就舍弃我记录的count，让它变为1，再进行判断和count++的操作，甚至我们还可以用它去求最长公共字串，一样的遇到不同的字母了，count就断掉，在后面再更新最大值就行了，那么代码如下：

#### 普通最大子数组和（有正有负）解法一：前缀和+贪心

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    vector<int> a(n + 1);
    for (int i = 1; i <= n; i++) cin >> a[i];
    vector<int> b(n + 1);
    b[0] = 0;
    for (int i = 1; i <= n; i++) b[i] = b[i - 1] + a[i]; // 构建一个前缀和数组
    int Max = b[1]; // 存储最大值，将它定为第一个元素
    int Min = b[0]; // 存储最小值，将它定为0
    for (int i = 1; i <= n; i++) {
        Max = max(Max, b[i] - Min); // 运用了贪心算法，计算当前前缀和减去最小值
        Min = min(Min, b[i]); // 存储当前最小值
    }
    cout << Max << endl;
    return 0;
}
```

#### 普通最大子数组和（有正有负）解法二：kadane算法

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    vector<int> dp;
    for (int i = 0; i < n; i++) {
        int num;
        cin >> num;
        dp.push_back(num);
    }
    int Max = dp[0];
    for (int i = 1; i < n; i++) {
        dp[i] = max(dp[i], dp[i - 1] + dp[i]); // 这一步就是我前面的最大值加现在的数和现在的数比较,就是如果说我[2,-1,-9,4,5],我到数字4的时候，如果加前面的一段就会负的更多，那我就要选择不加前面的一段，就是窗口后移到4的地方重新开始计算
        Max = max(Max, dp[i]); // 然后再不断更新我的最大值
    }
    cout << Max << endl;
    return 0;
}
```

**补：思考我在求子数组问题上加上一个条件：能被k整除的最大子数组和？**

> **作业6-10**
>
> **题目描述**
> 给定一个长度为 n 的正整数数组，以及一个正整数 k。 请你找出和最大的连续子数组，满足这个和能被 k 整除。 如果不存在这样的子数组，请输出 -1。
>
> **输入格式**
> 第一行两个整数 (n, k) 第二行 n 个正整数 (a_i)
>
> **输出格式**
> 一行一个整数，表示答案。 若无合法方案输出 -1
>
> **输入样例**
> ```
> 5 7
> 1 2 3 4 5
> ```
> **输出样例**
> ```
> 14
> ```
> **输入样例**
> ```
> 3 7
> 1 2 3
> ```
> **输出样例**
> ```
> -1
> ```

这个题其实也可以暴力解的，就是用双重循环来看：

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int n, k;
    cin >> n >> k;
    vector<int> a(n);
    for (int i = 0; i < n; ++i) {
        cin >> a[i];
    }
    long long ans = -1;  // 初始化最终输出 -1，表示没有符合条件的
    for (int i = 0; i < n; ++i) {
        long long sum = 0;
        for (int j = i; j < n; ++j) {
            sum += a[j]; // 这个其实是以a[i]为前缀往后看
            if (sum % k == 0) { // 满足就加入
                ans = max(ans, sum); // 不断更新最大值
            }
        }
    }
    cout << ans << endl;
    return 0;
}
```

上述的解法其实比较慢O(n^2)，事实上我们在思考加上一个条件能不能一样用前缀和或kadane算法呢？也就是说我们在这里可以想到同余原理，就是说我们先算前缀和，那么我们要让那一段子数组和最大就是前缀和的前后相减的差值最大，且同是要满足那段差值是k的倍数，也就是需要用到两个数除以 k 余数相同它们的差值也就一定能被 k 整除（类似于同余原理），我就先让前一项前缀和模k取余数，然后向后找和它模出来的余数相同的前缀和和前一个前缀和相减，再动态取最大值就可，把找子数组可模k变成了找相同余数问题后面的就和普通子数组问题一致了，前缀和解法（O(n)时间复杂度）如下：

```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    ll n, k;
    cin >> n >> k;
    vector<ll> a(n + 1);
    for (int i = 1; i <= n; i++) cin >> a[i];
    // 计算前缀和数组
    vector<ll> b(n + 1);
    for (int i = 1; i <= n; i++) b[i] = b[i - 1] + a[i];
    // 哈希表：键为前缀和取模后的余数，值为该余数对应的最小前缀和
    unordered_map<ll, ll> ans;
    ans[0] = 0; // 前缀和为0时，余数为0，这是初始状态
    ll res = 0; // 记录最大合法子数组和
    for (int i = 1; i <= n; i++) {
        // 计算当前前缀和模 k 的余数
        ll num = b[i] % k;
        // 处理负数取模的情况（保证余数在 [0, k-1] 范围内）
        if (num < 0) num += k;
        // 同余原理：如果之前出现过相同的余数，说明区间和能被 k 整除
        if (ans.find(num) != ans.end()) {
            // 当前前缀和 减去 之前的最小前缀和，得到最大合法子数组和
            res = max(res, b[i] - ans[num]);
        }
        // 维护哈希表：如果余数从未出现过，直接存入；如果出现过，则保留最小前缀和
        // 因为前缀和越小，与当前前缀和相减得到的差值越大
        if (ans.find(num) == ans.end()) {
            ans[num] = b[i];
        } else {
            ans[num] = min(ans[num], b[i]);
        }
    }
    // 如果结果仍为初始值 0，说明没有找到符合要求的子数组，输出 -1
    cout << (res == 0 ? -1 : res) << endl;
    return 0;
}
```

再看这个能不能用kadane算法呢？试想一下，我们以一个数为子数组的结尾，往前看，加和再和当前数比较取最大，不断更新最大值，这是kadane算法的核心，加上一个要求使加和是k的倍数，那么我们还是同余原理，就是以当前数为子数组末尾，且加和能%k==0，去求它的最大值，那我们用一个哈希表dp去存以当前数为末尾的且加和是k的倍数的当前最大子数组，就是键为余数，值为满足条件的子数组当前最大加和，但我们因为会有前面一段加和小于当前数去舍弃那一段的情况，所以我们要开多个哈希表来看在同一个余数k的情况下子数组累加和的变化，再最后更行并取最大值，代码如下：

```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, k;
    cin >> n >> k;
    vector<int> a(n + 1);
    for (int i = 1; i <= n; i++) cin >> a[i];
    // 改版Kadane的核心：按余数记录最大和
    unordered_map<int, ll> dp;
    dp[0] = 0; // 初始的时候：和为0，余数也为0
    ll res = -1; // 记录最终答案：最大合法子数组和，无解输出 -1
    for (int i = 1; i <= n; i++) {
        int x = a[i];
        // 如果不选当前数，那就直接继承上一轮所有状态就是说不和前面拼接，重新开始
        unordered_map<int, ll> ndp = dp;
        // 遍历上一轮所有余数状态
        for (auto &p : dp) {
            int m = p.first; // 表示上一个状态的余数
            ll s = p.second; // 表示上一个状态的最大和
            // 同余原理：计算拼接当前数后的新余数
            // +k再%k防止出现负数
            int num = (m + x % k + k) % k;
            // 用贪心更新：同一个余数，只保留最大的累加和
            // 如果找到和他余数相同的
            if (ndp.count(num)) ndp[num] = max(ndp[num], s + x); // 之前以num为余数的子数组最大和和现在的取最大值
            else ndp[num] = s + x; // 第一次出现，直接赋值
        }
        // 用新状态覆盖旧状态
        dp.swap(ndp); // 更新我们当前的状态(swap交换两个容器)
        // 如果余数=0，就说明子数组和能被k整除
        // 但i>=1时才是我们需要的解，dp[0]状态不为0时才输出
        if (dp.count(0) && dp[0] > 0) res = max(res, dp[0]);
    }
    cout << res << endl;
    return 0;
}
```

### ②. 和不大于目标值k的最长子数组

代码和思路如下：

#### 如果全是正数，就用滑动窗口

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, k;
    cin >> n >> k;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    int left = 0; // 滑动窗口的左边界
    int sum = 0, Max = 0;
    for (int right = 0; right < n; right++) {
        sum += a[right]; // 窗口右边界向右移动
        while (sum > k) { // 窗口内的和超过了K
            sum -= a[left]; // 窗口左边界向右移动，就是吐出来左边的数
            left++; // 让左指针向右移动，减小窗口大小
        }
        Max = max(Max, right - left + 1); // 更新最大长度
    }
    cout << Max << endl;
    return 0;
}
```

#### 如果有负数和0

这个为啥就不能用滑动窗口了呢？举个例子 `[3, -2, 5, -1], k=5`，就是说我们的左指针先不动，右指针一直右移，找到的第一个满足条件的是 `[3]`（长度1），然后右移加-2，窗口 `[3,-2]` 和=1（长度2），再右移加5，窗口 `[3,-2,5]` 和=6大于k了，按照我们的想法，要让左指针右移，把3吐出来，窗口变成 `[-2,5]` 和=3（长度2），再加入-1，窗口就变成了 `[-2,5,-1]` 和=2（长度3）。最终我们求出来的最大长度为3，但是正确答案是整个数组 `[3,-2,5,-1]` 和=5（长度4）。

为什么不对了？因为当窗口 `[3,-2,5]` 和超标时，我们把左边的3扔掉了，以为后面再也用不上3了。可是后面来了个-1，把和拉低了，这时候如果3还在，整个数组的和正好是5，长度4。但是滑动窗口的左指针只能往右走，扔掉的3再也回不来了。所以有负数的时候，滑动窗口会因为“暂时的超标”而错误地丢弃左边可能被后面负数救回来的元素。这就是不能用它的原因。

所以说这道题我们要用前缀和+哈希表：

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, k;
    cin >> n >> k;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    unordered_map<int, int> mp;
    mp[0] = -1;  // 用前缀和 0 出现在 -1 位置
    int sum = 0, Max = 0;
    for (int i = 0; i < n; i++) {
        sum += a[i]; // 创造前缀和
        if (mp.count(sum - k)) { // 找到前缀和减去k的差，就是满足条件的子数组的开始位置
            Max = max(Max, i - mp[sum - k]); // 更新最大长度，i-mp[sum - k]为当前子数组的长度
        }
        if (!mp.count(sum)) { // 只存第一次出现的位置（保证最长）
            mp[sum] = i; // 存前缀和出现的位置,避免重复
        }
    }
    cout << Max << endl;
    return 0;
}
```

### ③. 长度小于等于k加和最大的子数组

代码和思路如下：

#### 长度 ≤ K，和最大的子数组（单调队列+前缀和）

第一我们在看前缀和模版的时候，其实就知道前缀和可以用来多次查询 `[l+1,r]` 区间的子数组的加和。所以说我们要求长度<=k的子数组和的最大值，本质上我们查询区间（限制条件是 `l-r<=k`）的加和最大值。我们可以向kadane算法一样，就是定一个后缀，然后看以后缀为结尾的窗口的加和最大值，然后更新最大值就可以了。就好像洛谷P1886滑动窗口/单调队列差不多的，我从队尾弹出一个数，以他为窗口的结尾，当然同时要判断窗口的长度，然后和上面说的一样，更新最大值就行了。

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, k;
    cin >> n >> k;
    vector<int> a(n + 1);
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }
    vector<long long> b(n + 1, 0);
    for (int i = 1; i <= n; i++) {
        b[i] = b[i - 1] + a[i]; // 创建一个前缀和数组
    }
    deque<int> q;
    q.push_back(0); // 创建一个单调队列,
    long long Max = LLONG_MIN;
    for (int i = 1; i <= n; i++) {
        while (!q.empty() && i - q.front() > k) { // 在对位取数的时候，要判断窗口的长度是否符合要求
            q.pop_front();
        }
        Max = max(Max, b[i] - b[q.front()]); // 然后再不断更新给我们的最大值
        while (!q.empty() && b[i] <= b[q.back()]) { // 这个是要维护我们单调队列的单调性，最后那个数比前的数小，就弹出
            q.pop_back();
        }
        q.push_back(i); // 添加当前数
    }
    cout << Max << endl;
    return 0;
}
```

### ④. 环形数组子数组和最大值

代码和思路如下：

#### 环形最大子数组和

解释一下，啥是环形数组，就是类似循环链表，到最后一个元素后再从第一个开始。事实上我在看这样的题目的时候，觉得可以double一下数组再求，但这样直接求是不对的，因为环形数组的子数组长度不能超过 n。比如 `[5,-3,5]`，double 后是 `[5,-3,5,5,-3,5]`，DP 会算出 `[5,-3,5,5] = 12`，但实际环形数组最大子数组是 `[5,5] = 10`（因为不能重复取元素）。

所以我们在考虑问题的时候，就可以思考这个题目可能出现的情况：
1. 最长子数组不跨首尾的时候，那就和普通的kadane算法一样直接求。
2. 那如果跨首尾，那我们正难则反，就是我们已经知道最大和的那一段是跨首尾的，那想想就知道最小的那一段就一定不跨首尾，所以我们用数组的总和减去最小的，不就是最大的吗，相当于我用一次kadane算法求最小值，一剪就行了。
3. 最后我们比较如上两种情况谁最大，输出他们的最大值就是答案了。

```cpp
#include <bits/stdc++.h>
using namespace std;
pair<int, int> getMaxMin(vector<int>& a) {
    int curMax = a[0], maxSum = a[0];
    int curMin = a[0], minSum = a[0];
    for (int i = 1; i < a.size(); i++) {
        curMax = max(a[i], curMax + a[i]);
        maxSum = max(maxSum, curMax);
        curMin = min(a[i], curMin + a[i]);
        minSum = min(minSum, curMin);
    }
    return {maxSum, minSum};
}
int maxSubarraySumCircular(vector<int>& nums) {
    int sum = 0;
    for (int x : nums) sum += x;
    auto [maxSum, minSum] = getMaxMin(nums);
    int cross = sum - minSum;
    return maxSum < 0 ? maxSum : max(maxSum, cross); // 这里是处理了全是负数的情况，如果全是负数，那就会出现cross为0的情况，这时候我们应该取的是最大的那个负数才对
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    cout << maxSubarraySumCircular(a) << endl;
    return 0;
}
```

### ⑤. 求子数组乘积的最大值

代码和思路如下：

#### 实现乘积最大子数组

事实上我在写这个题目的时候，我还是想到了kadane算法，本质上我就是想将它加和求最大值的思路移到乘法来。就是我定义一个mul=a[0]，然后从前面乘，如果当前乘积<当前数，就舍弃，然后从当前数开始乘，最后更新最大值。不过这样是有巨大的bug的，因为乘积有一条就是负负得正，我如果在前面发现负数太大，比当前数小了，那我一定会舍弃它，但是万一后面也有个负数，负负得正了，和前面的负数乘起来可能就是我们要求的最大值，那我在前面就舍弃了，那最终结果就不对了。

比如说 `[-2, 3, -4]`，其实正确的是24，按我上面说的就会算出3，所以我们其实应该如下想：那我们其实可以加入当前最小值，就是说因为我们乘积有负负得正的特性，那我们把当前最小值加进来判断，在这个过程中，最小值是有可能在后面变成最大值的，保存它就可以更精准，最后在不断更新最大值就可，可以说这还是kadane算法的延申。

那我们思考一下，如果是除呢？除的情况要不要考虑最小值呢？其实还是要的，还是 `[-2, 3, -4]`，我们只记录最大值去比较的话就：因为-2/3肯定比3小，那我们舍弃它，求出来的值就是-3/4，实际上正确的是1/6，而且我们在除的时候也要考虑为0的情况哦！

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    int Max = a[0];
    int Min = a[0];
    int res = a[0];
    for (int i = 1; i < n; i++) {
        int tempMax = Max;
        int tempMin = Min;
        Max = max(max(tempMax * a[i], tempMin * a[i]), a[i]);
        Min = min(min(tempMax * a[i], tempMin * a[i]), a[i]);
        res = max(res, Max);
    }
    cout << res << endl;
    return 0;
}
```
#### 改编题：商最大子数组
#### 商最大子数组

#### 题目背景

本题无背景

#### 题目描述

给定一个长度为 n 的整数数组 nums（所有元素均不为 0) ,请找出数组中商最大的非空连续子数组，返回该子数组对应的商。

子数组是数组中一段连续的元素。

子数组的商定义为：从左到右依次进行除法运算。即若子数组为 [a1,a2,…,ak]（k≥1),则其商为：a1/a2/a3/…/ak

运算顺序严格按照从左到右，不可调换顺序，不可添加括号。

单个元素的商等于该元素本身。

除法运算中除数始终不为 0（输入保证所有元素非零）。

注意：商可能是负数，最大商指代数最大值,例如 (3>0.5>−1 ）,自己去总结要保留的小数位。

#### 输入格式

第一行一个整数 n

第二行 n 个整数，表示数组 nums

#### 输出格式

输出一个浮点数，表示最大商的数值。

#### 输入输出样例 #1

#### 输入 #1

```
4
-12 -2 -3 4
```

#### 输出 #1

```
6.0
```

#### 输入输出样例 #2

#### 输入 #2

```
4
-100 -1 2 3
```

#### 输出 #2

```
100.0
```
在这里我们就可以思考:对于前面的乘法和这里的除法最大子数组，其实也就是多了一个:
除法的除数不能为0，而这个题目已经说明了除数不为0，那么解题的过程就一样一样的了
都是双状态下的kadane算法，不过就是把乘号改为除号而已，所以代码如下：

```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long double ld; // 使用 long double 提高浮点数精度，防止小数点后丢失
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);   
    int n;
    cin >> n;
    vector<ld> a(n); // 存储输入的实数数组
    for (int i = 0; i < n; i++) cin >> a[i]; 
    // 初始化：以第一个元素作为初始的最大值、最小值和最终结果
    ld Max = a[0]; 
    ld Min = a[0];
    ld res = a[0];
    for (int i = 1; i < n; i++) { // 从第二个元素开始遍历
        // 因为做除法时，结果可能受前一步的最大值和最小值影响
        // （例如负数除以负数会变成大正数），所以先备份上一轮的状态
        ld tempMax = Max;
        ld tempMin = Min;
        // 计算当前最大可能值：
        // 1. 上一轮最大值 / 当前数
        // 2. 上一轮最小值 / 当前数（比如负负得正，或者除 0 导致无穷，必须一并考虑）
        // 3. 只取当前数自己（如果前面除法和当前数比更差，则重新开始）
        Max = max(max(tempMax / a[i], tempMin / a[i]), a[i]);
        // 计算当前最小可能值（同上，最小值也可能是由除以 0 或负数得来，为了后续反转必须保存）
        Min = min(min(tempMax / a[i], tempMin / a[i]), a[i]); 
        // 每轮更新全局最大结果
        res = max(res, Max);
    }
    // 设置输出格式：固定小数点，保留 1 位小数
    cout << fixed << setprecision(1) << res << endl;
    return 0;
}
```
所以对于加减乘除的运算我们可以搞出更有综合意味的一道题:
#### 四则运算连续最大子数组
#### 题目描述

给定一个全部元素均不为 0 的浮点（double）数组。你可以任选数组中一段非空的连续子数组，在选出的子数组中，每两个相邻数字之间，自由选择填入 +、-、*、/ 四种运算符 中的一个。

计算规则如下：
不允许添加任何括号，严格遵循数学默认运算优先级：先计算所有乘、除法，再计算所有加、减法。

除法采用浮点数除法（例如 7.0 / -2.0 = -3.5）。

仅包含单个数字的子数组，计算结果就是数字本身。

子数组是可以包含原数组所有元素的。

请你求出所有可选方案里，表达式能够算出的最大数值。

#### 输入格式

第一行输入一个整数 n(n<250)，代表数组的元素总个数。

第二行输入n个浮点数，为数组的全部元素。

#### 输出格式
输出一个浮点数值，表示能够算出的最大结果，结果精确到小数点后一位。

#### 输入样例
2

1.5 2.0

#### 输出样例
3.5

#### 输入样例
3

1.0 0.001 0.001

#### 输出样例
1000000.0

#### 说明：
最大值为1.0/0.001/0.001=1000000.0

首先先看代码:
```cpp
#include <bits/stdc++.h>
#include <iomanip>
using namespace std;
const int MAXN = 500;
const double INF = 1e18;
const double EPS = 1e-12;
double a[MAXN];
// dp_max[l][r]：区间 [l, r] 所能算出的最大结果
// dp_min[l][r]：区间 [l, r] 所能算出的最小结果
// 之所以要同时维护最小和最大，是因为减法中“减去最小数”会得到最大结果，以及乘除法中的“负负得正”现象。
double dp_max[MAXN][MAXN], dp_min[MAXN][MAXN];
int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }
    // 初始化答案为一个极小值
    double ans = -INF;
    // 1. 初始化长度为 1 的区间（单个数本身既是最大值也是最小值）
    for(int i = 1; i <= n; i++){
        dp_max[i][i] = a[i];
        dp_min[i][i] = a[i];
        ans = max(ans, dp_max[i][i]);
    }
    // 2. 按区间长度 len 从小到大进行 DP（从 2 开始，因为长度 1 已经初始化了）
    for(int len = 2; len <= n; len++){
        // 枚举区间的左端点 l，保证区间不越界
        for(int l = 1; l + len - 1 <= n; l++){
            int r = l + len - 1; // 计算区间的右端点
            // 3. 初始化当前区间的最大和最小值（赋为极小/极大值，方便后面取 max 和 min）
            dp_max[l][r] = -INF;
            dp_min[l][r] = INF;
            // 4. 枚举断点 k，在 k 和 k+1 之间放一个加法或减法符号
            for(int k = l; k < r; k++){
                // 用左区间 [l, k] 和右区间 [k+1, r] 的所有极值组合，尝试计算区间 [l, r] 的极
                // 加号组合 (左极大 + 右极大，左极大 + 右极小，... )
                double t1 = dp_max[l][k] + dp_max[k+1][r];
                double t2 = dp_max[l][k] + dp_min[k+1][r];
                double t3 = dp_min[l][k] + dp_max[k+1][r];
                double t4 = dp_min[l][k] + dp_min[k+1][r];
                // 减号组合 (左极大 - 右极小，左极小 - 右极大，... 注意减法对正负非常敏感)
                double t5 = dp_max[l][k] - dp_max[k+1][r];
                double t6 = dp_max[l][k] - dp_min[k+1][r];
                double t7 = dp_min[l][k] - dp_max[k+1][r];
                double t8 = dp_min[l][k] - dp_min[k+1][r]; 
                // 综合 8 种情况，找出当前断点下可能的最大值和最小值
                double now_max = max({t1,t2,t3,t4,t5,t6,t7,t8});
                double now_min = min({t1,t2,t3,t4,t5,t6,t7,t8})
                // 更新该区间 [l, r] 的全局 DP 状态
                dp_max[l][r] = max(dp_max[l][r], now_max);
                dp_min[l][r] = min(dp_min[l][r], now_min);
            }
            // 5. 特殊情况：枚举从 l 到 r 的“纯乘除段”（不使用加减号，只会用 * 和 /，且严格从左算到右）
            // 初始化乘除段的最大和最小值
            double mul_max = a[l];
            double mul_min = a[l];
            // 从 l+1 依次乘或除以每个数
            for(int p = l + 1; p <= r; p++){
                // 乘除运算同样需要考虑极值反转（负数乘负数变正数，正数除以极小数变极大数）
                double n1 = mul_max * a[p]; // 当前最大值 * 当前数
                double n2 = mul_min * a[p]; // 当前最小值 * 当前数
                double n3 = mul_max / a[p]; // 当前最大值 / 当前数
                double n4 = mul_min / a[p]; // 当前最小值 / 当前数
                double nmx = max({n1,n2,n3,n4}); // 更新本段当前最大值
                double nmn = min({n1,n2,n3,n4}); // 更新本段当前最小值
                mul_max = nmx;
                mul_min = nmn;
            }
            // 将“纯乘除段”也能达到的极值，合并到区间 [l, r] 的 DP 状态中
            dp_max[l][r] = max(dp_max[l][r], mul_max);
            dp_min[l][r] = min(dp_min[l][r], mul_min);
            // 不断更新全局的最大答案
            ans = max(ans, dp_max[l][r]);
        }
    }
    // 保留一位小数输出答案
    cout << fixed << setprecision(1) << ans << endl;
    return 0;
}
```
#### 首先说说这道题目的难点在哪
这道题的核心难点在于：**不允许添加括号，且必须严格遵循“先算乘除，再算加减”的数学优先级规则。**

如果直接做普通的动态规划，会遇到以下困难：
1. **无法直接确定断点**：因为我们可以在任意两个数字之间自由选择 `+`、`-`、`*`、`/`，所以加减号的出现位置是不确定的。
2. **负负得正与减号反转**：在处理表达式时，比如 `A - B`，如果 `B` 是极小值（负数），减去它反而会得到非常大的正数。这要求我们在 DP 中必须同时维护最大值和最小值。
3. **乘除的连续性与优先级**：因为乘除优先于加减，这意味着一段连续的乘除运算必须被绑定在一起，不能被加减号打断。

其实在这里就要引入**区间dp**了:
由于 \(N < 250\)，数据规模比较小，我们可以使用 **区间动态规划（区间 DP）** 来穷举所有可能的结构。

**状态定义：**
*   `dp_max[l][r]`：表示子数组区间 `[l, r]` 能够算出的 **最大结果**。
*   `dp_min[l][r]`：表示子数组区间 `[l, r]` 能够算出的 **最小结果**。

**初始状态：**
*   对于长度只有 1 的区间（即单个数字），`dp_max[i][i] = dp_min[i][i] = a[i]`。

我们在考虑区间 `[l, r]` 的结果时，表达式只存在两种基本形态：

#### 1. 形态一：由加减号切分（枚举断点 k）
只要表达式中有加减号，我们就可以把 `[l, r]` 拆分成左半部分 `[l, k]` 和右半部分 `[k+1, r]`，中间用一个加号或减号连接。

根据 DP 的完备性，我们需要考虑左半部分、右半部分的最大值和最小值，总共 **8 种拼接情况**：
*   **使用加号（4种）**：
    *   `dp_max[l][k] + dp_max[k+1][r]`
    *   `dp_max[l][k] + dp_min[k+1][r]`
    *   `dp_min[l][k] + dp_max[k+1][r]`
    *   `dp_min[l][k] + dp_min[k+1][r]`
*   **使用减号（4种）**：
    *   `dp_max[l][k] - dp_max[k+1][r]`
    *   `dp_max[l][k] - dp_min[k+1][r]`
    *   `dp_min[l][k] - dp_max[k+1][r]`
    *   `dp_min[l][k] - dp_min[k+1][r]`

取这 8 种情况的最大值和最小值，作为当前 `k` 断点下的候选答案。
#### 2. 形态二：纯乘除段（不包含加减号）
因为加减号优先级的限制，当区间 `[l, r]` 中没有加减号时，它必须是一个**连续的乘除表达式**。
我们初始化 `mul_max = a[l]`，`mul_min = a[l]`，然后遍历 `p = l+1` 到 `r`，依次尝试乘、除当前数字：

*   当前最大乘 `a[p]`：`mul_max * a[p]`
*   当前最小乘 `a[p]`：`mul_min * a[p]`
*   当前最大除 `a[p]`：`mul_max / a[p]`
*   当前最小除 `a[p]`：`mul_min / a[p]`

取上述 4 种情况的极值，更新 `mul_max` 和 `mul_min`。这就是纯乘除段的最优解。
#### 四、最终合并

对于区间 `[l, r]`：
1. 遍历每一个断点 `k`，用 8 种加减法组合更新状态。
2. 计算纯乘除段的最优解。
3. 将两者的最大值合并进 `dp_max[l][r]`（最小值合并进 `dp_min[l][r]`）。

最后，遍历所有区间的 `dp_max[l][r]`，取全局最大值即为答案。
#### 复杂度分析

*   **时间复杂度**：\(O(N^4)\)。外层两个循环枚举区间（\(O(N^2)\)），内层枚举断点（\(O(N)\)），再乘以内部计算纯乘除段的循环（\(O(N)\)）。虽然理论上是 \(O(N^4)\)，但因为 \(N < 250\) 且常数较小，可以轻松通过。
*   **空间复杂度**：\(O(N^2)\)，用于存储 DP 表。

### ⑥位运算里的子数组问题
### 最大连续子数组异或和
### 题目描述
给定长度为 n 的整数数组 a，求其中**连续子数组的异或和的最大值**。
- 连续子数组：原数组中一段连续的元素
- 子数组异或和：该区间所有元素依次异或运算的结果
- $1 \le n \le 10^5$
- $0 \le a[i] \le 2^{30}$

### 输入格式
第一行一个整数 n，表示数组长度

第二行 n 个整数，表示数组 a

### 输出格式
一个整数，表示最大连续子数组异或和

### 样例
#### 样例 1
#### 输入样例

4

1 2 3 4

#### 输出样例

7

解释：子数组 `[3,4]`，$3 \oplus 4 = 7$

#### 样例 2
#### 输入样例

4

8 1 2 12

#### 输出样例

15


### 解题思路提示
1. 前缀异或公式
    - $pre[0]=0,\;pre[i]=pre[i-1] \oplus a[i]$
    - 区间 $[l,r]$ 异或和 = $pre[r] \oplus pre[l-1]$
    - 问题转化为在前缀异或数组里，求任意两个前缀值的最大异或值
2. 01字典树
    - 从高位到低位存储前缀异或的二进制
    - 遍历每个前缀异或值时，贪心查找能产生最大异或的历史前缀
    - 时间复杂度：$O(31 \times n)$，适配大数据

还是一样先看代码:
```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
// 定义 01 字典树的节点结构体
struct TrieNode {
    TrieNode* child[2]; // 两个子节点：child[0] 表示当前位是 0，child[1] 表示当前位是 1
    TrieNode() {        // 构造函数：初始化节点
        child[0] = child[1] = nullptr; // 初始时 0 和 1 两个子节点都为空
    }
};
// 向字典树中插入一个数字
// 作用：将前缀异或和的 31 位二进制形式从高位到低位挂到树上
void insert(TrieNode* root, ll num) {
    TrieNode* cur = root;             // 从根节点开始走
    for (int i = 30; i >= 0; --i) {   // 遍历 30 位到 0 位（因为 a[i] <= 2^30）
        int bit = (num >> i) & 1LL;   // 提取数字 num 在当前第 i 位是 0 还是 1
        if (!cur->child[bit]) {       // 如果这条路径（这个 bit）之前没走过
            cur->child[bit] = new TrieNode(); // 就新创建一个节点
        }
        cur = cur->child[bit];        // 指针下移，走到新创建的节点上
    }
}
// 查询与 num 异或值最大的数
// 作用：在已有的前缀异或集合中，找哪个数和当前的 num 异或的结果最大
ll query(TrieNode* root, ll num) {
    TrieNode* cur = root;             // 从根节点开始走
    ll max_xor = 0;                   // 记录最大的异或结果
    for (int i = 30; i >= 0; --i) {   // 同样从高位到低位遍历
        int bit = (num >> i) & 1LL;   // 提取当前数字在第 i 位是 0 还是 1
        int want = 1 - bit;           // 贪心策略：我希望找到相反的数（0 找 1，1 找 0），这样异或结果就是 1
        if (cur->child[want]) {       // 如果树上存在相反的路径
            max_xor |= (1LL << i);    // 异或结果的这一位就定死为 1，累加到结果里
            cur = cur->child[want];   // 走到相反的路径上
        } else {                      // 如果树上不存在相反的路径
            cur = cur->child[bit];    // 只能无奈走相同的路径（异或结果为 0），这也是最优解
        }
    }
    return max_xor;                   // 返回找到的最大异或值
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    vector<int> a(n);
    TrieNode* root = new TrieNode();  // 创建字典树的根节点
    insert(root, 0LL);                // 必须先插入 0！因为这代表 pre[0]，即从数组头开始算的子数组
    ll pre = 0, ans = 0;              // pre 存当前前缀异或和，ans 存最终答案
    for (int i = 0; i < n; i++) {
        cin >> a[i];
        pre ^= a[i];                                  // 计算当前位的前缀异或和       
        ans = max(ans, query(root, pre));             // 查询历史前缀中能和它组成最大异或和的，更新答案       
        insert(root, pre);                            // 将这个前缀异或和插入字典树，供后续数字查询使用
    }
    cout << ans << endl;
    return 0;
}
```
### 解题思路：最大连续子数组异或和（前缀异或 + 01字典树）

#### 1. 核心转化：前缀异或
利用异或的性质（a ^ a = 0），我们可以预处理出**前缀异或和**：
- 定义 pre[i] 为原数组前 i 个元素的异或和：`pre[i] = pre[i-1] ^ a[i]`
- 特别地，`pre[0] = 0`
- 对于任意区间 [l, r]，其异或和可以表示为：`pre[r] ^ pre[l-1]`

**所以，求“原数组中任意连续区间的最大异或和”，等价于：**
在从左到右的遍历过程中，对于每一个**当前的 `pre[i]`**，在**它之前出现过的历史前缀** `pre[0]...pre[i-1]` 中，找到一个 `pre[j]`，使得 `pre[i] ^ pre[j]` 的值最大。

#### 2. 为什么必须“在线查询”（非常关键）？
如果一开始就把所有 `pre[0]...pre[n]` 全部插入字典树，再对每个 `pre[i]` 去查，可能会查到 `pre[i]` 后面的前缀（比如 `pre[i+1]`）。这样算出来的异或值对应的区间 `[i+1, i]` 根本不合法（右端点在左端点之前）。

因此必须保证：在查询 `pre[i]` 时，字典树里**只能有下标小于 i 的前缀**。这就决定了我们必须在从左到右遍历的过程中，动态地“查询一个，插入一个”。

#### 3. 数据结构：01 字典树（Trie）
为了让“找到历史前缀中异或值最大的那个数”更快，我们用 **01 字典树** 来存储历史前缀。
- 字典树每个节点有两个子节点（0 和 1）。
- **插入**：将每个前缀异或值的二进制（30 ~ 0 位）按顺序插入字典树。
- **查询**：拿着当前的 `pre[i]`，从根节点开始往下走。在每一位上，如果树中**有与当前位相反的路径**，就走相反路径（异或结果该位为 1），否则只能走相同路径（异或结果该位为 0）。

#### 4. 贪心思想：高位优先
在二进制中，越高的位对数值大小的影响越巨大。
- 只要高位能异或出 1，即使低位全为 0，也一定比高位为 0、低位全为 1 的数大。
- 因此我们从第 30 位到第 0 位遍历，只要存在相反的位，就一定要选相反的位，保证最大异或值。

#### 5. 算法流程
1. 初始化字典树，并插入 `pre[0] = 0`（代表从数组开头开始算的区间）。
2. 从 1 到 n 遍历原数组，依次计算当前前缀异或和 `pre[i]`。
3. 用当前的 `pre[i]` 去查询字典树里已有的历史前缀，得到最大异或值，更新全局最大值 `ans`。
4. 将当前的 `pre[i]` 插入字典树，供后续的前缀查询。
5. 遍历结束后，输出 `ans`。

#### 6. 时间复杂度与空间复杂度分析
- **时间复杂度**：每个数都要插入一次和查询一次，每次操作需要遍历固定的 31 个二进制位，因此总复杂度为 **O(31 * n) = O(n)**，完美适配 n <= 10^5。
- **空间复杂度**：最多有 n+1 个前缀，每个前缀拆成 31 位插入，最多约 31 * n 个节点，空间复杂度为 **O(31 * n)**。

### 题目二:位运算连续最大子数组
#### 题目描述

给定一个全部元素**均不为 0** 的 **64 位无符号整数数组**。

你可以任选数组中一段**非空连续子数组**，在选出的子数组中，每两个相邻数字之间，自由选择填入 `&`、`|`、`^` 三种位运算符中的一个。

#### 计算规则

- 所有运算符遵循以下**隐式优先级**（优先级决定了运算顺序）：
    1. **最高优先级**：`^`（异或），优先完成所有异或运算；
    2. **次优先级**：`&` 和 `|`（与、或）二者**同级**，同级运算符出现时，**严格从左向右依次计算**；
    3. 所有运算均按**位运算规则**进行（即对64位无符号整数的每一位独立运算）。
- **仅包含单个数字的子数组**，表达式结果等于数字本身。
- 子数组可取原数组任意连续区间（包含整个数组）。

设 `f(l, r)` 代表子数组 `[l, r]` 通过任意运算符组合能够算出的**最大数值**，请你求出全局所有方案中的最大数值。

#### 输入格式

第一行输入一个整数 `n`，代表数组的元素总个数。  
第二行输入 `n`个**较大**整数，为数组全部元素。

#### 输出格式

输出一个整数，表示能够算出的**最大结果**。

#### 输入输出样例 #1

#### 输入 #1

```
2
1 2
```

#### 输出 #1

```
3
```

#### 输入输出样例 #2

#### 输入 #2

```
3
1 1000 1000
```

#### 输出 #2

```
1001
```

#### 输入输出样例 #3

#### 输入 #3

```
4
1 2 4 8
```

#### 输出 #3

```
15
```

#### 说明/提示

#### 样例解释

| 样例 | 数组              | 最优表达式         | 求值过程                                                     | 结果 |
| ---- | ----------------- | ------------------ | ------------------------------------------------------------ | ---- |
| #1   | `[1, 2]`          | `1 | 2` 或 `1 ^ 2` | `1 | 2 = 3`；`1 ^ 2 = 3`                                     | 3    |
| #2   | `[1, 1000, 1000]` | `1 ^ 1000 | 1000`  | ① 先算异或：`1 ^ 1000 = 1001`② 再算或：`1001 | 1000 = 1001`  | 1001 |
| #3   | `[1, 2, 4, 8]`    | `1 | 2 ^ 4 | 8`    | ① 先算异或：`2 ^ 4 = 6`② 同级或从左到右：　`1 | 6 = 7`　`7 | 8 = 15` | 15   |

#### 更多说明

- 样例 #1 中，`1 & 2 = 0` 不是最优，最大值为 `3`。
- 样例 #2 中，其他组合如 `1 | 1000 ^ 1000` 的求值过程为：先算 `1000 ^ 1000 = 0`，再算 `1 | 0 = 1`，小于最优值 `1001`。
- 样例 #3 中，`^` 优先级最高，因此 `2 ^ 4` 先结合，之后 `1` 和 `8` 与中间结果进行同级 `|` 运算，从左到右执行。

#### 数据范围与约定

- 对于 $30\%$ 的数据，$1 \le n \le 50$；
- 对于 $60\%$ 的数据，$1 \le n \le 500$；
- 对于 $100\%$ 的数据，$1 \le n \le 10^5$，$0 < a_i < 2^{64}$。

#### 解题思路
是一个找规律的过程，因为当我们对照位运算表的时候就发现了，虽然这道题目的异或是优先的，但是我们在考虑最大值的时候，在考虑了全局的所有构造就会发现尽可能用或运算才能达到最大值，因为只要知道与运算会消去1，而异或会翻转1和0的情况，而或运算却能使结果的二进制的1个数更多，这样其实就打破了这个障眼法了，这个题目也就更多地转化为或最大值子数组了
所以代码就很容易可解了:
```java
import java.io.*;
import java.util.*;
public class Main {
    public static void main(String[] args) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        PrintWriter out = new PrintWriter(System.out);
        int n = Integer.parseInt(br.readLine());
        StringTokenizer st = new StringTokenizer(br.readLine());
        long ans = 0;
        for (int i = 0; i < n; i++) {
            ans |= Long.parseUnsignedLong(st.nextToken());
        }
        out.println(Long.toUnsignedString(ans));
        out.close();
    }
}
```
### ⑦其他问题
### 最大逆序子数组
### 题目描述

给定一个长度为 $n$ 的数组 $a$。
对于任意连续子数组 $[l,r]$，定义 $f(l,r)$ 为该区间内部的逆序对数量（满足 $l \le i<j \le r,\ a_i>a_j$ 的有序对总数）。
请你求出所有**非空**且长度**不大于`k`连续**子数组中 $f(l,r)$ 的最大值。

### 输入格式

第一行两个整数 $n$,$k$。

第二行 $n$ 个整数，表示一个数组。

### 输出格式

输出一行一个整数，表示最大的**区间逆序数**。

### 输入输出样例 #1

### 输入 #1

```in
4 4
4 1 3 2
```

### 输出 #1

```out
4
```

### 说明/提示

对于 100% 的数据：n ≤ 10⁵，1≤ k ≤ 500

保证所有数字在 int 范围内

可以，下面直接给你 **Markdown 源码**，你复制即可。

### 解题思路

#### 1. 关键性质

题目要求所有长度**不超过 `k`** 的连续子数组中，逆序对数量的最大值。

对于一个连续区间 `[l,r]`，如果向其中加入一个新的元素，例如扩展为：

```text
[l-1,r]
````

或者：

```text
[l,r+1]
```

原区间中的所有逆序对都不会消失，同时新加入的元素可能与原区间中的元素形成新的逆序对。

因此有：

$$
f(l,r)\le f(l-1,r)
$$

以及：

$$
f(l,r)\le f(l,r+1)
$$

也就是说，**随着区间不断扩大，逆序对数量不会减少**。

因此，如果一个区间长度小于 `k`，那么我们总可以继续扩展它，直到长度达到 `k`，并且逆序对数量不会变小。

所以：

> 所有长度不超过 `k` 的区间中，逆序对数量的最大值一定可以由某个长度恰好为 `k` 的连续子数组取得。

当 `k > n` 时，显然直接取整个数组即可，因此实际窗口长度为：

```text
len = min(n, k)
```

---

#### 2. 问题转化

原问题：

> 求所有长度不超过 `k` 的连续子数组中的最大逆序对数量。

可以转化为：

> 求所有长度恰好为 `len = min(n,k)` 的连续子数组中的最大逆序对数量。

因此可以使用**滑动窗口**进行维护。

---

#### 3. 如何维护窗口中的逆序对数量？

使用**离散化 + 树状数组**。

设当前窗口的逆序对数量为：

```text
cnt
```

##### 初始化第一个窗口

从左到右依次加入元素 `a[i]`。

当加入 `a[i]` 时，它只能作为逆序对的右端点，因此需要统计当前窗口中有多少元素满足：

$$
a_j>a_i
$$

假设当前窗口已经有 `i` 个元素，其中不大于 `a[i]` 的元素数量为：

```text
query(rank(a[i]))
```

那么大于 `a[i]` 的元素数量就是：

$$
i-query(rank(a[i]))
$$

将其加入 `cnt` 即可。

---

#### 4. 滑动窗口

假设当前窗口为：

```text
[l,r]
```

窗口向右移动后变成：

```text
[l+1,r+1]
```

需要进行两次操作。

##### 删除左端点 `a[l]`

由于 `a[l]` 在原窗口中始终位于最左边，因此它形成的逆序对一定满足：

$$
a_l>a_j
$$

其中：

$$
l<j\le r
$$

所以删除 `a[l]` 时，需要从 `cnt` 中减去当前窗口中**小于 `a[l]` 的元素数量**。

即：

```text
query(rank(a[l]) - 1)
```

然后将 `a[l]` 从树状数组中删除。

##### 加入新的右端点 `a[r+1]`

新元素位于窗口最右边，因此它形成的逆序对一定满足：

$$
a_i>a_{r+1}
$$

因此需要统计当前窗口中**大于 `a[r+1]` 的元素数量**。

假设当前窗口中有 `len-1` 个元素，其中不大于 `a[r+1]` 的元素数量为：

```text
query(rank(a[r+1]))
```

那么大于它的元素数量为：

$$
len-1-query(rank(a[r+1]))
$$

将这个数量加入 `cnt`，然后将新元素加入树状数组即可。

每次滑动后更新最大答案。

---

#### 5. 复杂度分析

**离散化需要**：

$$
O(n\log n)
$$

每个元素最多进行常数次树状数组操作，每次操作复杂度为：

$$
O(\log n)
$$

因此总时间复杂度为：

$$
O(n\log n)
$$

空间复杂度为：

$$
O(n)
$$

* 代码如下:
```cpp
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
int n, k;
vector<int> a, tree;
void add(int x, int v) {
    while (x < (int)tree.size()) {
        tree[x] += v;
        x += x & -x;
    }
}
int query(int x) {
    int res = 0;
    while (x) {
        res += tree[x];
        x -= x & -x;
    }
    return res;
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cin >> n >> k;
    a.resize(n);
    vector<int> b(n);
    for (int i = 0; i < n; i++) {
        cin >> a[i];
        b[i] = a[i];
    }
    sort(b.begin(), b.end());
    b.erase(unique(b.begin(), b.end()), b.end());
    for (int i = 0; i < n; i++) {
        a[i] = lower_bound(b.begin(), b.end(), a[i]) - b.begin() + 1;
    }
    int m = b.size();
    tree.assign(m + 1, 0);
    k = min(k, n);
    ll count = 0;
    // 初始化第一个长度为 k 的窗口
    for (int i = 0; i < k; i++) {
        count += i - query(a[i]);
        add(a[i], 1);
    }
    ll ans = count;
    // 滑动窗口
    for (int l = 0, r = k; r < n; l++, r++) {
        // 删除左端点
        count -= query(a[l] - 1);
        add(a[l], -1);
        // 加入右端点
        count += (k - 1) - query(a[r]);
        add(a[r], 1);
        ans = max(ans, count);
    }
    cout << ans << '\n';
    return 0;
}
```

### 课后写题

> **引用：PTA算法1-7~9 连续子序列最大和**
>
> 给定 n 个整数组成的序列 { a1, a2, ..., an }，“连续子序列”被定义为 { ai, ai+1, ..., aj }，其中 1≤i≤j≤n。“连续子序列最大和”则被定义为所有连续子序列元素的和中最大者。例如给定序列 { -2, 11, -4, 13, -5, -2 }，其连续子序列 { 11, -4, 13 } 有最大的和 20。请编写程序，计算给定整数序列的连续子序列最大和。
>
> 本题旨在测试各种不同的算法在各种数据情况下的表现。各组测试数据特点如下：
> 数据 0~6：测试基本正确性；
> 数据 7：10^3 个随机整数；
> 数据 8：10^4 个随机整数；
> 数据 9：10^5 个随机整数。
>
> 输入格式：
> 输入第一行给出正整数 n (≤10^5)；第二行给出 n 个整数，绝对值均不超过 100，其间以空格分隔。
>
> 输出格式：
> 在第一行中输出连续子序列最大和，第二行输出该子序列首尾的数组下标（从 0 开始），以 1 个空格分隔。若解不唯一，则输出最小的数组下标（如样例所示）。
> 注意：如果序列中所有整数皆为零或负数，则取空子列的结果是最大的，为 0；此时空子序列数组首尾的下标均为 -1。
>
> **输入样例：**
> ```
> 10
> -10 2 2 3 4 -5 -23 4 7 -21
> ```
> **输出样例：**
> ```
> 11
> 1 4
> ```

这个题多出来它子数组的端点输出，联系老登算法(kadane算法），在前面因为有负数让加和小于当前末尾值了的时候，我就把子数组的左边界到当前位置，持续记录就可，代码如下：

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    vector<int> a(n);
    // 标记：数组中是否存在正数
    // 作用：如果全是负数/0，最大子序列和为0，且下标输出-1 -1
    bool hasPositive = false;
    for (int i = 0; i < n; ++i) {
        cin >> a[i];
        // 只要有一个数 > 0，就标记为true
        if (a[i] > 0) hasPositive = true;
    }
    // 如果数组中没有正数（全负 或 全0）
    if (!hasPositive) {
        // 根据题目要求：输出最大和0，空子序列下标-1 -1
        cout << "0\n-1 -1\n";
        return 0;
    }
    int maxSum = a[0];    // 记录全局最大子数组和
    int curSum = 0;       // 记录当前连续子数组的和
    int bestStart = 0;    // 最优子数组的起点下标
    int bestEnd = 0;      // 最优子数组的终点下标
    int curStart = 0;     // 当前正在累加的子数组起点
    // 遍历数组，动态更新最大和与区间
    for (int i = 0; i < n; ++i) {
        // 如果当前和为负数，说明前面的子数组拖累结果，重新开始计算
        if (curSum < 0) {
            curSum = a[i];      // 从当前数字重新开始
            curStart = i;       // 记录新起点
        } else {
            curSum += a[i];     // 否则继续累加当前数字
        }
        // 如果当前子数组和 > 已知最大值，更新最大值和最优区间
        if (curSum > maxSum) {
            maxSum = curSum;
            bestStart = curStart;
            bestEnd = i;
        }
    }
    cout << maxSum << "\n" << bestStart << " " << bestEnd << "\n";
    return 0;
}
```

> **引用：牛客HIGH68 【模板】双指针（这可不是简单的老登哦！）**
>
> **描述**
> 对于给定的长度为 n 的数组 {a1, a2, ..., an}，找出最长的区间，满足区间中元素两两不同。
> 如果有多个这样的区间，依次输出它们。
>
> **输入描述：**
> 第一行输入一个整数 n (1 <= n <= 2 * 10^5) 代表数组中的元素数量。
> 第二行输入 n 个整数 a1, a2, ..., an (0 <= ai <= n) 代表初始数组。
>
> **输出描述：**
> 第一行输出一个整数 m (1 <= m <= n) 代表满足条件的区间数量。
> 此后 m 行，每行输出两个整数 l, r (1 <= l <= r <= n) 代表满足条件的区间。本题没有 SPJ，请按照 l 递增的顺序输出。
>
> **示例1**
> 输入：
> ```
> 6
> 1 1 4 5 1 4
> ```
> 输出：
> ```
> 3
> 2 4
> 3 5
> 4 6
> ```

这个题我在看的时候其实可用到kadane算法的思想(老登再次上线！哈哈），怎么说呢，题目说要求最长且满足两两不等的子数组最大长度，本质上就是和kadane一样，我遍历数组用左右指针来计录，如果有 `nums[left]=nums[right]` 情况了，那我的count就断掉，把left对应的元素吐出去，right++把新元素放进来，用MaxLen来记录它当前满足条件的最大长度，不断更行就行了，那如果有多个要记录下标，那我用一个List（int[]类型的）来存每一段满足条件的子数组，把长度为最大（前面求出来的）的那些子数组都找出来，把他们的下标输出就可，当然我们在判断子数组里是不是两两不同可以用一个容器来存，比如哈希表，HashMap来存，用containsKey来判断就可，代码如下：

```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        PrintWriter out = new PrintWriter(System.out);
        StringTokenizer st = new StringTokenizer(br.readLine());
        int n = Integer.parseInt(st.nextToken());
        int[] arr = new int[n];
        st = new StringTokenizer(br.readLine());
        for (int i = 0; i < n; i++) {
            arr[i] = Integer.parseInt(st.nextToken());
        }
        HashMap<Integer, Integer> map = new HashMap<>();
        ArrayList<int[]> list = new ArrayList<>();
        int Maxlen = Integer.MIN_VALUE;
        int left = 0;
        for (int right = 0; right < n; right++) {
            int num = arr[right]; // 我先拿到我右指针所在的数字
            if (map.containsKey(num)) { // 判断数字是否在map中
                int index = map.get(num); // 找到它的下标
                if (index >= left) { // 只要他不是原先那个left了
                    left = index + 1; // 就把放到当前的下标+1，为啥是加1呢，因为index的那个位置是表示一以现过的，我们要取它的后一位
                    // 举个例子，[1，2，3，2，4]，我发现第二个2进来后，原先的left就要移到数字3的位置，就是第一个2下标加1.
                }
            }
            map.put(num, right); // 把left下标更新后，再把第二个2加进去。
            int len = right - left + 1; // 计算长度
            if (len > Maxlen) {
                Maxlen = len;
                // 为啥要clear?因为列表里有满足条件却长度不是最大长度的，要删去它们。
                list.clear();
            }
            if (len == Maxlen) {
                list.add(new int[]{left + 1, right + 1}); // 当满足条件且长度为最大的再加入列表，题目要求从1开始，那我们要加个1.
            }
        }
        out.println(list.size());
        for (int[] a : list) {
            out.println(a[0] + " " + a[1]); // 取出我存入的下标。
        }
        out.flush();
        br.close();
        out.close();
    }
}
```

